---
title: waggle
description: A capture orchestrator that reads URLs from Postgres and submits them to BrowserHive.
template: splash
hero:
  tagline: Reads URLs from a Postgres table and tells the hive what to capture.
  actions:
    - text: Quickstart
      link: /waggle/quickstart/
      icon: right-arrow
      variant: primary
    - text: BrowserHive Docs
      link: https://uraitakahito.github.io/browserhive/
      icon: external
---

## What waggle is

waggle answers one question: **which URLs get captured**. It selects the enabled
rows of a Postgres `urls` table and submits one `POST /v1/captures` per row to a
[BrowserHive](https://uraitakahito.github.io/browserhive/) server, in parallel.

The name comes from the [waggle dance](https://en.wikipedia.org/wiki/Waggle_dance)
bees use to point hive-mates at nectar.

## What waggle is not

It never drives a browser. It takes no screenshots, builds no WACZ, writes
nothing to S3 — **that is all BrowserHive's job**, and this site does not
document it. Anything about _how_ a page is captured lives in
[BrowserHive's documentation](https://uraitakahito.github.io/browserhive/).

The interaction is **fire-and-forget**: BrowserHive answers `202` with a
`taskId` and runs the capture asynchronously on its worker pool. waggle records
which submissions were accepted and exits. Following a task to completion is not
implemented (Stage 1 of the roadmap); the outcome shows up in BrowserHive's log
and in the artifact store.

## Where things are

| I want to…                       | Page                                                        |
| -------------------------------- | ----------------------------------------------------------- |
| run a capture for the first time | [Quickstart](/waggle/quickstart/)                           |
| set up the dev environment       | [Development environment](/waggle/development-environment/) |
| manage the URL list              | [URL source](/waggle/url-source/)                           |
| shape how a run captures         | [Capture options](/waggle/capture-options/)                 |
| move to a newer BrowserHive      | [Upgrading BrowserHive](/waggle/upgrading-browserhive/)     |
| understand the moving parts      | [Architecture](/waggle/architecture/)                       |
