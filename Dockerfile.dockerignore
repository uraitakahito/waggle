# Whitelist mode: BuildKit reads `<dockerfile>.dockerignore` in preference
# to `.dockerignore`. We start by excluding everything, then re-include
# only what `Dockerfile`'s COPY directives need.
*

!package.json
!pnpm-lock.yaml
!pnpm-workspace.yaml
!tsconfig.json
!tsconfig.build.json
!src/
