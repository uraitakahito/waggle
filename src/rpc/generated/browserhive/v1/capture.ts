// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: browserhive/v1/capture.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

/** capture の結末。 */
export enum CaptureStatus {
  CAPTURE_STATUS_UNSPECIFIED = 0,
  CAPTURE_STATUS_SUCCESS = 1,
  CAPTURE_STATUS_FAILED = 2,
  CAPTURE_STATUS_TIMEOUT = 3,
  CAPTURE_STATUS_HTTP_ERROR = 4,
  UNRECOGNIZED = -1,
}

export function captureStatusFromJSON(object: any): CaptureStatus {
  switch (object) {
    case 0:
    case "CAPTURE_STATUS_UNSPECIFIED":
      return CaptureStatus.CAPTURE_STATUS_UNSPECIFIED;
    case 1:
    case "CAPTURE_STATUS_SUCCESS":
      return CaptureStatus.CAPTURE_STATUS_SUCCESS;
    case 2:
    case "CAPTURE_STATUS_FAILED":
      return CaptureStatus.CAPTURE_STATUS_FAILED;
    case 3:
    case "CAPTURE_STATUS_TIMEOUT":
      return CaptureStatus.CAPTURE_STATUS_TIMEOUT;
    case 4:
    case "CAPTURE_STATUS_HTTP_ERROR":
      return CaptureStatus.CAPTURE_STATUS_HTTP_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CaptureStatus.UNRECOGNIZED;
  }
}

export function captureStatusToJSON(object: CaptureStatus): string {
  switch (object) {
    case CaptureStatus.CAPTURE_STATUS_UNSPECIFIED:
      return "CAPTURE_STATUS_UNSPECIFIED";
    case CaptureStatus.CAPTURE_STATUS_SUCCESS:
      return "CAPTURE_STATUS_SUCCESS";
    case CaptureStatus.CAPTURE_STATUS_FAILED:
      return "CAPTURE_STATUS_FAILED";
    case CaptureStatus.CAPTURE_STATUS_TIMEOUT:
      return "CAPTURE_STATUS_TIMEOUT";
    case CaptureStatus.CAPTURE_STATUS_HTTP_ERROR:
      return "CAPTURE_STATUS_HTTP_ERROR";
    case CaptureStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** GetCapture の進行。 */
export enum CaptureState {
  CAPTURE_STATE_UNSPECIFIED = 0,
  CAPTURE_STATE_PENDING = 1,
  CAPTURE_STATE_PROCESSING = 2,
  CAPTURE_STATE_DONE = 3,
  UNRECOGNIZED = -1,
}

export function captureStateFromJSON(object: any): CaptureState {
  switch (object) {
    case 0:
    case "CAPTURE_STATE_UNSPECIFIED":
      return CaptureState.CAPTURE_STATE_UNSPECIFIED;
    case 1:
    case "CAPTURE_STATE_PENDING":
      return CaptureState.CAPTURE_STATE_PENDING;
    case 2:
    case "CAPTURE_STATE_PROCESSING":
      return CaptureState.CAPTURE_STATE_PROCESSING;
    case 3:
    case "CAPTURE_STATE_DONE":
      return CaptureState.CAPTURE_STATE_DONE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CaptureState.UNRECOGNIZED;
  }
}

export function captureStateToJSON(object: CaptureState): string {
  switch (object) {
    case CaptureState.CAPTURE_STATE_UNSPECIFIED:
      return "CAPTURE_STATE_UNSPECIFIED";
    case CaptureState.CAPTURE_STATE_PENDING:
      return "CAPTURE_STATE_PENDING";
    case CaptureState.CAPTURE_STATE_PROCESSING:
      return "CAPTURE_STATE_PROCESSING";
    case CaptureState.CAPTURE_STATE_DONE:
      return "CAPTURE_STATE_DONE";
    case CaptureState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 署名の検査 1 つぶんの結果。 */
export enum CheckOutcome {
  CHECK_OUTCOME_UNSPECIFIED = 0,
  CHECK_OUTCOME_OK = 1,
  CHECK_OUTCOME_FAILED = 2,
  CHECK_OUTCOME_SKIPPED = 3,
  UNRECOGNIZED = -1,
}

export function checkOutcomeFromJSON(object: any): CheckOutcome {
  switch (object) {
    case 0:
    case "CHECK_OUTCOME_UNSPECIFIED":
      return CheckOutcome.CHECK_OUTCOME_UNSPECIFIED;
    case 1:
    case "CHECK_OUTCOME_OK":
      return CheckOutcome.CHECK_OUTCOME_OK;
    case 2:
    case "CHECK_OUTCOME_FAILED":
      return CheckOutcome.CHECK_OUTCOME_FAILED;
    case 3:
    case "CHECK_OUTCOME_SKIPPED":
      return CheckOutcome.CHECK_OUTCOME_SKIPPED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CheckOutcome.UNRECOGNIZED;
  }
}

export function checkOutcomeToJSON(object: CheckOutcome): string {
  switch (object) {
    case CheckOutcome.CHECK_OUTCOME_UNSPECIFIED:
      return "CHECK_OUTCOME_UNSPECIFIED";
    case CheckOutcome.CHECK_OUTCOME_OK:
      return "CHECK_OUTCOME_OK";
    case CheckOutcome.CHECK_OUTCOME_FAILED:
      return "CHECK_OUTCOME_FAILED";
    case CheckOutcome.CHECK_OUTCOME_SKIPPED:
      return "CHECK_OUTCOME_SKIPPED";
    case CheckOutcome.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** worker の健康状態。 */
export enum WorkerHealth {
  WORKER_HEALTH_UNSPECIFIED = 0,
  WORKER_HEALTH_READY = 1,
  WORKER_HEALTH_BUSY = 2,
  WORKER_HEALTH_ERROR = 3,
  WORKER_HEALTH_DISCONNECTED = 4,
  UNRECOGNIZED = -1,
}

export function workerHealthFromJSON(object: any): WorkerHealth {
  switch (object) {
    case 0:
    case "WORKER_HEALTH_UNSPECIFIED":
      return WorkerHealth.WORKER_HEALTH_UNSPECIFIED;
    case 1:
    case "WORKER_HEALTH_READY":
      return WorkerHealth.WORKER_HEALTH_READY;
    case 2:
    case "WORKER_HEALTH_BUSY":
      return WorkerHealth.WORKER_HEALTH_BUSY;
    case 3:
    case "WORKER_HEALTH_ERROR":
      return WorkerHealth.WORKER_HEALTH_ERROR;
    case 4:
    case "WORKER_HEALTH_DISCONNECTED":
      return WorkerHealth.WORKER_HEALTH_DISCONNECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WorkerHealth.UNRECOGNIZED;
  }
}

export function workerHealthToJSON(object: WorkerHealth): string {
  switch (object) {
    case WorkerHealth.WORKER_HEALTH_UNSPECIFIED:
      return "WORKER_HEALTH_UNSPECIFIED";
    case WorkerHealth.WORKER_HEALTH_READY:
      return "WORKER_HEALTH_READY";
    case WorkerHealth.WORKER_HEALTH_BUSY:
      return "WORKER_HEALTH_BUSY";
    case WorkerHealth.WORKER_HEALTH_ERROR:
      return "WORKER_HEALTH_ERROR";
    case WorkerHealth.WORKER_HEALTH_DISCONNECTED:
      return "WORKER_HEALTH_DISCONNECTED";
    case WorkerHealth.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 失敗の分類。 */
export enum ErrorType {
  ERROR_TYPE_UNSPECIFIED = 0,
  ERROR_TYPE_HTTP = 1,
  ERROR_TYPE_TIMEOUT = 2,
  ERROR_TYPE_CONNECTION = 3,
  ERROR_TYPE_SIGNING = 4,
  ERROR_TYPE_INTERNAL = 5,
  UNRECOGNIZED = -1,
}

export function errorTypeFromJSON(object: any): ErrorType {
  switch (object) {
    case 0:
    case "ERROR_TYPE_UNSPECIFIED":
      return ErrorType.ERROR_TYPE_UNSPECIFIED;
    case 1:
    case "ERROR_TYPE_HTTP":
      return ErrorType.ERROR_TYPE_HTTP;
    case 2:
    case "ERROR_TYPE_TIMEOUT":
      return ErrorType.ERROR_TYPE_TIMEOUT;
    case 3:
    case "ERROR_TYPE_CONNECTION":
      return ErrorType.ERROR_TYPE_CONNECTION;
    case 4:
    case "ERROR_TYPE_SIGNING":
      return ErrorType.ERROR_TYPE_SIGNING;
    case 5:
    case "ERROR_TYPE_INTERNAL":
      return ErrorType.ERROR_TYPE_INTERNAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ErrorType.UNRECOGNIZED;
  }
}

export function errorTypeToJSON(object: ErrorType): string {
  switch (object) {
    case ErrorType.ERROR_TYPE_UNSPECIFIED:
      return "ERROR_TYPE_UNSPECIFIED";
    case ErrorType.ERROR_TYPE_HTTP:
      return "ERROR_TYPE_HTTP";
    case ErrorType.ERROR_TYPE_TIMEOUT:
      return "ERROR_TYPE_TIMEOUT";
    case ErrorType.ERROR_TYPE_CONNECTION:
      return "ERROR_TYPE_CONNECTION";
    case ErrorType.ERROR_TYPE_SIGNING:
      return "ERROR_TYPE_SIGNING";
    case ErrorType.ERROR_TYPE_INTERNAL:
      return "ERROR_TYPE_INTERNAL";
    case ErrorType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * この取り込みが、前のタスクの残留物を持ち越すかどうか。
 *
 * 0 番に ISOLATED を置いているのは意図的で、この repo の他の enum が守っている
 * `_UNSPECIFIED = 0` の慣習を破っている。proto3 のスカラ enum は「未設定」と「0」を
 * 区別できないので、0 を ISOLATED にすれば「指定しなかった」と「隔離を頼んだ」が
 * 同じものになる —— つまり **「指定しなかったのに持ち越されていた」が表現できなく
 * なる**。かつて cache と reset_state という 3 つのつまみでそれを表そうとして、
 * どれも origin ストレージを覆っておらず、www.yahoo.co.jp のフィードが
 * アーカイブに入らないことに 1 年近く誰も気づかなかった。
 */
export enum SessionMode {
  /**
   * SESSION_MODE_ISOLATED - 既定。使い捨ての BrowserContext で取り込む。cookie / HTTP キャッシュ /
   * localStorage / sessionStorage / IndexedDB / Service Worker がすべて空から始まる。
   */
  SESSION_MODE_ISOLATED = 0,
  /**
   * SESSION_MODE_SHARED - worker が持ち回る BrowserContext とタブを使う。ログイン後の連続取り込みなど、
   * 持ち越すことが目的のときだけ選ぶ。同じ worker に載った **無関係なタスクにも
   * 状態が漏れる** ことを承知の上で。後始末は一切しない。
   */
  SESSION_MODE_SHARED = 1,
  UNRECOGNIZED = -1,
}

export function sessionModeFromJSON(object: any): SessionMode {
  switch (object) {
    case 0:
    case "SESSION_MODE_ISOLATED":
      return SessionMode.SESSION_MODE_ISOLATED;
    case 1:
    case "SESSION_MODE_SHARED":
      return SessionMode.SESSION_MODE_SHARED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionMode.UNRECOGNIZED;
  }
}

export function sessionModeToJSON(object: SessionMode): string {
  switch (object) {
    case SessionMode.SESSION_MODE_ISOLATED:
      return "SESSION_MODE_ISOLATED";
    case SessionMode.SESSION_MODE_SHARED:
      return "SESSION_MODE_SHARED";
    case SessionMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 取得する形式。少なくとも 1 つは true でなければならない (handlers が検証する)。 */
export interface CaptureFormats {
  png: boolean;
  webp: boolean;
  html: boolean;
  links: boolean;
  mhtml: boolean;
  wacz: boolean;
}

/** 大きな固定オーバーレイを消す第 2 パスの閾値。省略時はサーバ既定。 */
export interface DismissHeuristicSpec {
  enabled?: boolean | undefined;
  minViewportCoverageRatio?: number | undefined;
  minZIndex?: number | undefined;
}

/** banner / modal の除去を 1 回の capture だけ調整する。 */
export interface DismissSpec {
  useDefaults?: boolean | undefined;
  extraSelectors: string[];
  excludeFrameworks: string[];
  heuristic?: DismissHeuristicSpec | undefined;
  failOnError?: boolean | undefined;
}

/**
 * viewport の指定。
 *
 * proto は値域を書けないので、範囲は server 側の境界 (request-limits.ts) が
 * 強制する。外れると INVALID_ARGUMENT。
 */
export interface Viewport {
  /** 1–7680 (8K の幅)。 */
  width: number;
  /** 1–4320 (8K の高さ)。 */
  height: number;
}

/**
 * 走らせるもの 1 つぶん。どれを走らせるかと、どう設定するかを 1 つにする。
 *
 * oneof にしているので、存在しない behavior は **綴れない**。かつては id が
 * ただの文字列で、`autoscrol` と打ち間違えても黙って通り、何も走らないまま
 * 取り込みが成功していた。
 */
export interface Behavior {
  autoscroll?: AutoScroll | undefined;
  autofetch?: AutoFetch | undefined;
  autoplay?: AutoPlay | undefined;
  custom?: Custom | undefined;
}

/** ページの全高までスクロールして、遅延読み込みを発火させる。 */
export interface AutoScroll {
  /** 既定 40。 */
  maxSteps?:
    | number
    | undefined;
  /** 既定 250。 */
  stepDelayMs?:
    | number
    | undefined;
  /** 既定 1000。 */
  idleTimeMs?: number | undefined;
}

/** srcset の全候補と data-* の遅延属性を能動的に取りに行く。 */
export interface AutoFetch {
  /** 既定 2000。 */
  maxUrls?: number | undefined;
}

/** <video> / <audio> をミュート再生して、その資源を取りに行く。 */
export interface AutoPlay {
  /** 既定 1000。 */
  settleMs?: number | undefined;
}

/**
 * client が持ち込む behavior。サーバが --allow-custom-behaviors で起動している
 * ときだけ効き、そうでなければ黙って無視される。
 */
export interface Custom {
  /** behavior report に出る識別子。 */
  id: string;
  /** { static id, static isMatch, run(ctx) } を実装した JS のクラス式。 */
  source: string;
  /**
   * この behavior に渡す値。中身が任意の JS である以上、その設定も型では守れない
   * —— JSON の逃げ道はここだけに閉じてある。
   */
  optionsJson?: string | undefined;
}

/**
 * 走らせるものの列。message で包んでいるのは `optional` を付けるため: proto3 の
 * repeated には presence が無く、空と未指定を区別できない。
 */
export interface BehaviorList {
  items: Behavior[];
}

/** capture ごとの behavior の指定。 */
export interface BehaviorSpec {
  /**
   * 走らせるものを、走らせる順に。
   *
   *   省略 → サーバ既定 (--behaviors) をそのまま走らせる
   *   items が空 → 1 つも走らせない
   *   並び → 走らせるものを **書き切ったもの**
   *
   * 最後の 1 行が肝で、「既定の集合はそのままで設定だけ差し替える」はできない。
   * かつては有効集合と設定が別々に届いたのでそれができ、同時に「集合を空に
   * したつもりが既定に戻っていた」も起きていた。
   */
  behaviors?:
    | BehaviorList
    | undefined;
  /**
   * 同梱の site behavior を対象にするか。省略するとサーバ既定 (出荷時は on)。
   * `behaviors` と別なのは性質が違うから —— site behavior は host を見る
   * isMatch() が門番で、client が並べるものではない。
   */
  siteBehaviors?: boolean | undefined;
}

export interface SubmitCaptureRequest {
  url: string;
  /**
   * 成果物のファイル名に入る札。使える文字の制限は無い —— 名前を組むときに
   * 区切りと衝突する文字は %XX へ逃がされるので、空白も `_` も `/` も日本語も
   * そのまま渡せる。制限は **合計の長さ** だけで、
   * `{taskId}_{correlationId}[_{labels}].result.json` が 255 UTF-8 バイトを
   * 超える要求は境界が拒む。逃がされる文字と非 ASCII は 1 文字 3 バイト。
   */
  labels: string[];
  /**
   * client 側の相関キー。labels と同じ規則でファイル名に入る。
   * 送らなかった場合も名前の枠は残る (`{taskId}__{labels}` のように下線が並ぶ) ——
   * 枠が固定されているので、名前から元の値を読み戻せる。
   */
  correlationId?: string | undefined;
  captureFormats?:
    | CaptureFormats
    | undefined;
  /**
   * 上流へ送る Accept-Language。印字可能な ASCII のみ、200 文字以下。
   * 制御文字 (CR/LF/NUL) は header への注入になるので境界が拒む。
   */
  acceptLanguage?:
    | string
    | undefined;
  /**
   * wacz-auth の署名を求める。`capture_formats.wacz` と一緒のときだけ有効で、
   * 他の組み合わせは INVALID_ARGUMENT で拒む。
   */
  signing?: boolean | undefined;
  dismissBannersEnabled?: boolean | undefined;
  dismissBannersSpec?:
    | DismissSpec
    | undefined;
  /**
   * リクエスト単位の viewport。値域 (1–7680 × 1–4320) は server 側の境界
   * (request-limits.ts) が強制する。
   */
  viewport?:
    | Viewport
    | undefined;
  /**
   * 0–5000。ページ操作 1 つごとに挟む遅延で、headless の描画を眺めるためのもの。
   * 待ちの手段ではないので上限が要る (1 操作ごとに効き、操作は十数回ある)。
   *
   * 操作ごとの timeout の予算の **外側** で数える —— わざと遅くした取り込みは
   * 固まった取り込みではない。効くのはタスク全体の予算だけ。
   */
  operationDelayMs?: number | undefined;
  trace?:
    | boolean
    | undefined;
  /**
   * 読み込む device pixel ratio を、読み込む順に。各要素は 1–3 の整数で
   * (2 が Retina、3 が現行の携帯端末の上)、同じ値を 2 度置くことはできない。
   *
   * 要素数がそのまま読み込みの回数になる。読み込むたびにバナー除去も behaviors も
   * 走るので、所要時間も WARC のバイト数も要素数に比例して増える。
   *
   * 順序に意味がある: PNG / WebP は読み込みが全部終わってから 1 度だけ撮るので、
   * **最後の要素**の倍率になる。[2, 1] と書けば画像は 1x で残る。
   *
   * 空ならサーバ既定 (--device-pixel-ratios)。proto3 の repeated は「空」と
   * 「未指定」を区別できないので、空は「指定なし」として扱うほかない。
   */
  devicePixelRatios: number[];
  fullPage?: boolean | undefined;
  behaviors?:
    | BehaviorSpec
    | undefined;
  /** 前のタスクの残留物を持ち越すか。省略 = ISOLATED (使い捨ての context)。 */
  session: SessionMode;
  /**
   * この取り込みに限って本文の上限を締める。省略時はサーバ既定 (GetStatus の
   * limits.max_response_bytes)。
   *
   * 動くのは締める方向だけで、既定より大きい値は既定に丸める。上限は資源を守る
   * ために運用者が置くもので、呼ぶ側が上げられるならそれは上限ではない。拒否では
   * なく丸めにしているのは、呼ぶ側が配備の既定を知らずに要求できるようにするため
   * —— 効いた値は archive の settings.limits.max_response_bytes に残るので、
   * 何が起きたかは後から読める。
   */
  maxResponseBytes?: number | undefined;
}

export interface GetCaptureRequest {
  taskId: string;
}

export interface GetStatusRequest {
  /**
   * 返す pending task の上限。0–200 で、省略時は 50。proto は値域を持てないので、
   * 既定を当てるのも範囲の検査も handler の仕事。
   */
  pendingLimit?: number | undefined;
}

export interface SubmitCaptureResponse {
  /** 失敗は status code で返るので、ここに来るのは受理されたときだけ。 */
  accepted: boolean;
  taskId: string;
  correlationId?: string | undefined;
}

/** 成果物の置き場所 (s3://…)。取得しなかった形式は空のまま。 */
export interface CaptureArtifacts {
  png?: string | undefined;
  webp?: string | undefined;
  html?: string | undefined;
  links?: string | undefined;
  mhtml?: string | undefined;
  wacz?: string | undefined;
}

export interface WaczStats {
  totalRecorded: number;
  totalBlocked: number;
  totalSkippedContentType: number;
  totalTruncatedTooLarge: number;
  totalTruncatedTaskCap: number;
  totalFailed: number;
  totalIncomplete: number;
  totalBodyBytes: number;
}

export interface WaczSignatureChecks {
  signature: CheckOutcome;
  chain: CheckOutcome;
  domain: CheckOutcome;
  timestamp: CheckOutcome;
}

export interface WaczSignature {
  signed: boolean;
  reason?: string | undefined;
  domain?: string | undefined;
  checks?: WaczSignatureChecks | undefined;
}

export interface WaczCompleteness {
  bodylessUrls: string[];
  truncatedUrls: string[];
  complete: boolean;
}

export interface CaptureErrorDetails {
  type: ErrorType;
  message: string;
  httpStatusCode?: number | undefined;
  httpStatusText?: string | undefined;
  timeoutMs?: number | undefined;
}

export interface CaptureResultReport {
  taskId: string;
  correlationId?: string | undefined;
  url: string;
  labels: string[];
  status: CaptureStatus;
  httpStatusCode?: number | undefined;
  timestamp: string;
  captureProcessingTimeMs: number;
  retryCount: number;
  workerIndex: number;
  artifacts?: CaptureArtifacts | undefined;
  waczStats?: WaczStats | undefined;
  completeness?: WaczCompleteness | undefined;
  signature?: WaczSignature | undefined;
  errorDetails?: CaptureErrorDetails | undefined;
}

/**
 * GetCapture の答え。state と report は独立していない —— report が入るのは state が
 * DONE のときだけで、その対応を protobuf で書く手段が無いため、型ではなく約束として
 * 守られている。
 */
export interface GetCaptureResponse {
  state: CaptureState;
  /**
   * 触る前に `state == DONE` を確かめること。`state != PENDING` では足りない ——
   * PENDING と PROCESSING はどちらも「まだ終わっていない」側。
   *
   * 確かめずに読むと status = 0 (CAPTURE_STATUS_UNSPECIFIED) を掴む。言語によっては
   * 落ちもしない。そして「SUCCESS ではない」は「失敗した」と区別が付かないので、
   * まだ走っている取り込みが失敗として台帳に載る。
   */
  report?: CaptureResultReport | undefined;
}

export interface ErrorTaskInfo {
  taskId: string;
  url: string;
  labels: string[];
}

export interface ErrorRecord {
  type: ErrorType;
  message: string;
  httpStatusCode?: number | undefined;
  httpStatusText?: string | undefined;
  timeoutMs?: number | undefined;
  timestamp: string;
  task?: ErrorTaskInfo | undefined;
}

export interface BrowserOptions {
  browserUrl: string;
}

export interface CurrentTask {
  taskId: string;
  url: string;
  labels: string[];
  correlationId?: string | undefined;
  startedAt: string;
  elapsedMs: number;
  retryCount: number;
}

export interface WorkerInfo {
  index: number;
  browserOptions?: BrowserOptions | undefined;
  health: WorkerHealth;
  processedCount: number;
  errorCount: number;
  errorHistory: ErrorRecord[];
  currentTask?: CurrentTask | undefined;
}

export interface PendingTask {
  taskId: string;
  url: string;
  labels: string[];
  correlationId?: string | undefined;
  enqueuedAt: string;
  queuedMs: number;
  retryCount: number;
}

export interface ProcessingTask {
  taskId: string;
  url: string;
  labels: string[];
  correlationId?: string | undefined;
  enqueuedAt: string;
  queuedMs: number;
  retryCount: number;
  workerIndex: number;
  startedAt: string;
  elapsedMs: number;
}

export interface QueueSnapshot {
  pendingTasks: PendingTask[];
  processingTasks: ProcessingTask[];
}

export interface BuildInfo {
  version: string;
  revision: string;
  buildTime: string;
}

/**
 * サーバが強いる境界。リクエストでは変えられないもの。
 *
 * 投げる前に「この大きさは切り詰められる」と分かるために在る。archive 側の
 * settings.limits と同じ値だが、答える問いが違う —— あちらは「何が起きたか」、
 * こちらは「これから何が起きるか」。
 *
 * 載せるのは境界だけ。viewport や devicePixelRatios はリクエストで上書きできるので
 * 境界ではなく、ここには来ない。設定の置き場にし始めたら、それは status とは
 * 別の RPC に切る合図。
 */
export interface ServerLimits {
  /** 1 レスポンスの本文の上限。超えた本文は落とし、切り詰めとして記録する。 */
  maxResponseBytes: number;
  /** 1 タスクが記録する本文の合計の上限。 */
  maxTaskBytes: number;
  /** 1 タスクが同時に抱える保留リクエストの上限。 */
  maxPendingRequests: number;
}

export interface GetStatusResponse {
  pending: number;
  processing: number;
  succeeded: number;
  failed: number;
  operationalWorkers: number;
  totalWorkers: number;
  isRunning: boolean;
  isDegraded: boolean;
  workers: WorkerInfo[];
  queue?: QueueSnapshot | undefined;
  build?: BuildInfo | undefined;
  limits?: ServerLimits | undefined;
}

function createBaseCaptureFormats(): CaptureFormats {
  return { png: false, webp: false, html: false, links: false, mhtml: false, wacz: false };
}

export const CaptureFormats: MessageFns<CaptureFormats> = {
  encode(message: CaptureFormats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.png !== false) {
      writer.uint32(8).bool(message.png);
    }
    if (message.webp !== false) {
      writer.uint32(16).bool(message.webp);
    }
    if (message.html !== false) {
      writer.uint32(24).bool(message.html);
    }
    if (message.links !== false) {
      writer.uint32(32).bool(message.links);
    }
    if (message.mhtml !== false) {
      writer.uint32(40).bool(message.mhtml);
    }
    if (message.wacz !== false) {
      writer.uint32(48).bool(message.wacz);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureFormats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCaptureFormats();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.png = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.webp = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.html = reader.bool();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.links = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.mhtml = reader.bool();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.wacz = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CaptureFormats {
    return {
      png: isSet(object.png) ? globalThis.Boolean(object.png) : false,
      webp: isSet(object.webp) ? globalThis.Boolean(object.webp) : false,
      html: isSet(object.html) ? globalThis.Boolean(object.html) : false,
      links: isSet(object.links) ? globalThis.Boolean(object.links) : false,
      mhtml: isSet(object.mhtml) ? globalThis.Boolean(object.mhtml) : false,
      wacz: isSet(object.wacz) ? globalThis.Boolean(object.wacz) : false,
    };
  },

  toJSON(message: CaptureFormats): unknown {
    const obj: any = {};
    if (message.png !== false) {
      obj.png = message.png;
    }
    if (message.webp !== false) {
      obj.webp = message.webp;
    }
    if (message.html !== false) {
      obj.html = message.html;
    }
    if (message.links !== false) {
      obj.links = message.links;
    }
    if (message.mhtml !== false) {
      obj.mhtml = message.mhtml;
    }
    if (message.wacz !== false) {
      obj.wacz = message.wacz;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureFormats>, I>>(base?: I): CaptureFormats {
    return CaptureFormats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureFormats>, I>>(object: I): CaptureFormats {
    const message = createBaseCaptureFormats();
    message.png = object.png ?? false;
    message.webp = object.webp ?? false;
    message.html = object.html ?? false;
    message.links = object.links ?? false;
    message.mhtml = object.mhtml ?? false;
    message.wacz = object.wacz ?? false;
    return message;
  },
};

function createBaseDismissHeuristicSpec(): DismissHeuristicSpec {
  return { enabled: undefined, minViewportCoverageRatio: undefined, minZIndex: undefined };
}

export const DismissHeuristicSpec: MessageFns<DismissHeuristicSpec> = {
  encode(message: DismissHeuristicSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== undefined) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.minViewportCoverageRatio !== undefined) {
      writer.uint32(17).double(message.minViewportCoverageRatio);
    }
    if (message.minZIndex !== undefined) {
      writer.uint32(24).int32(message.minZIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DismissHeuristicSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDismissHeuristicSpec();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.enabled = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.minViewportCoverageRatio = reader.double();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.minZIndex = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DismissHeuristicSpec {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : undefined,
      minViewportCoverageRatio: isSet(object.minViewportCoverageRatio)
        ? globalThis.Number(object.minViewportCoverageRatio)
        : isSet(object.min_viewport_coverage_ratio)
        ? globalThis.Number(object.min_viewport_coverage_ratio)
        : undefined,
      minZIndex: isSet(object.minZIndex)
        ? globalThis.Number(object.minZIndex)
        : isSet(object.min_z_index)
        ? globalThis.Number(object.min_z_index)
        : undefined,
    };
  },

  toJSON(message: DismissHeuristicSpec): unknown {
    const obj: any = {};
    if (message.enabled !== undefined) {
      obj.enabled = message.enabled;
    }
    if (message.minViewportCoverageRatio !== undefined) {
      obj.minViewportCoverageRatio = message.minViewportCoverageRatio;
    }
    if (message.minZIndex !== undefined) {
      obj.minZIndex = Math.round(message.minZIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DismissHeuristicSpec>, I>>(base?: I): DismissHeuristicSpec {
    return DismissHeuristicSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DismissHeuristicSpec>, I>>(object: I): DismissHeuristicSpec {
    const message = createBaseDismissHeuristicSpec();
    message.enabled = object.enabled ?? undefined;
    message.minViewportCoverageRatio = object.minViewportCoverageRatio ?? undefined;
    message.minZIndex = object.minZIndex ?? undefined;
    return message;
  },
};

function createBaseDismissSpec(): DismissSpec {
  return {
    useDefaults: undefined,
    extraSelectors: [],
    excludeFrameworks: [],
    heuristic: undefined,
    failOnError: undefined,
  };
}

export const DismissSpec: MessageFns<DismissSpec> = {
  encode(message: DismissSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.useDefaults !== undefined) {
      writer.uint32(8).bool(message.useDefaults);
    }
    for (const v of message.extraSelectors) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.excludeFrameworks) {
      writer.uint32(26).string(v!);
    }
    if (message.heuristic !== undefined) {
      DismissHeuristicSpec.encode(message.heuristic, writer.uint32(34).fork()).join();
    }
    if (message.failOnError !== undefined) {
      writer.uint32(40).bool(message.failOnError);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DismissSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDismissSpec();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.useDefaults = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.extraSelectors.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.excludeFrameworks.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.heuristic = DismissHeuristicSpec.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.failOnError = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DismissSpec {
    return {
      useDefaults: isSet(object.useDefaults)
        ? globalThis.Boolean(object.useDefaults)
        : isSet(object.use_defaults)
        ? globalThis.Boolean(object.use_defaults)
        : undefined,
      extraSelectors: globalThis.Array.isArray(object?.extraSelectors)
        ? object.extraSelectors.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.extra_selectors)
        ? object.extra_selectors.map((e: any) => globalThis.String(e))
        : [],
      excludeFrameworks: globalThis.Array.isArray(object?.excludeFrameworks)
        ? object.excludeFrameworks.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.exclude_frameworks)
        ? object.exclude_frameworks.map((e: any) => globalThis.String(e))
        : [],
      heuristic: isSet(object.heuristic) ? DismissHeuristicSpec.fromJSON(object.heuristic) : undefined,
      failOnError: isSet(object.failOnError)
        ? globalThis.Boolean(object.failOnError)
        : isSet(object.fail_on_error)
        ? globalThis.Boolean(object.fail_on_error)
        : undefined,
    };
  },

  toJSON(message: DismissSpec): unknown {
    const obj: any = {};
    if (message.useDefaults !== undefined) {
      obj.useDefaults = message.useDefaults;
    }
    if (message.extraSelectors?.length) {
      obj.extraSelectors = message.extraSelectors;
    }
    if (message.excludeFrameworks?.length) {
      obj.excludeFrameworks = message.excludeFrameworks;
    }
    if (message.heuristic !== undefined) {
      obj.heuristic = DismissHeuristicSpec.toJSON(message.heuristic);
    }
    if (message.failOnError !== undefined) {
      obj.failOnError = message.failOnError;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DismissSpec>, I>>(base?: I): DismissSpec {
    return DismissSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DismissSpec>, I>>(object: I): DismissSpec {
    const message = createBaseDismissSpec();
    message.useDefaults = object.useDefaults ?? undefined;
    message.extraSelectors = object.extraSelectors?.map((e) => e) || [];
    message.excludeFrameworks = object.excludeFrameworks?.map((e) => e) || [];
    message.heuristic = (object.heuristic !== undefined && object.heuristic !== null)
      ? DismissHeuristicSpec.fromPartial(object.heuristic)
      : undefined;
    message.failOnError = object.failOnError ?? undefined;
    return message;
  },
};

function createBaseViewport(): Viewport {
  return { width: 0, height: 0 };
}

export const Viewport: MessageFns<Viewport> = {
  encode(message: Viewport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).int32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Viewport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseViewport();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.width = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.height = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Viewport {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: Viewport): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Viewport>, I>>(base?: I): Viewport {
    return Viewport.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Viewport>, I>>(object: I): Viewport {
    const message = createBaseViewport();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseBehavior(): Behavior {
  return { autoscroll: undefined, autofetch: undefined, autoplay: undefined, custom: undefined };
}

export const Behavior: MessageFns<Behavior> = {
  encode(message: Behavior, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.autoscroll !== undefined) {
      AutoScroll.encode(message.autoscroll, writer.uint32(10).fork()).join();
    }
    if (message.autofetch !== undefined) {
      AutoFetch.encode(message.autofetch, writer.uint32(18).fork()).join();
    }
    if (message.autoplay !== undefined) {
      AutoPlay.encode(message.autoplay, writer.uint32(26).fork()).join();
    }
    if (message.custom !== undefined) {
      Custom.encode(message.custom, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Behavior {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBehavior();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.autoscroll = AutoScroll.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.autofetch = AutoFetch.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.autoplay = AutoPlay.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.custom = Custom.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Behavior {
    return {
      autoscroll: isSet(object.autoscroll) ? AutoScroll.fromJSON(object.autoscroll) : undefined,
      autofetch: isSet(object.autofetch) ? AutoFetch.fromJSON(object.autofetch) : undefined,
      autoplay: isSet(object.autoplay) ? AutoPlay.fromJSON(object.autoplay) : undefined,
      custom: isSet(object.custom) ? Custom.fromJSON(object.custom) : undefined,
    };
  },

  toJSON(message: Behavior): unknown {
    const obj: any = {};
    if (message.autoscroll !== undefined) {
      obj.autoscroll = AutoScroll.toJSON(message.autoscroll);
    }
    if (message.autofetch !== undefined) {
      obj.autofetch = AutoFetch.toJSON(message.autofetch);
    }
    if (message.autoplay !== undefined) {
      obj.autoplay = AutoPlay.toJSON(message.autoplay);
    }
    if (message.custom !== undefined) {
      obj.custom = Custom.toJSON(message.custom);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Behavior>, I>>(base?: I): Behavior {
    return Behavior.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Behavior>, I>>(object: I): Behavior {
    const message = createBaseBehavior();
    message.autoscroll = (object.autoscroll !== undefined && object.autoscroll !== null)
      ? AutoScroll.fromPartial(object.autoscroll)
      : undefined;
    message.autofetch = (object.autofetch !== undefined && object.autofetch !== null)
      ? AutoFetch.fromPartial(object.autofetch)
      : undefined;
    message.autoplay = (object.autoplay !== undefined && object.autoplay !== null)
      ? AutoPlay.fromPartial(object.autoplay)
      : undefined;
    message.custom = (object.custom !== undefined && object.custom !== null)
      ? Custom.fromPartial(object.custom)
      : undefined;
    return message;
  },
};

function createBaseAutoScroll(): AutoScroll {
  return { maxSteps: undefined, stepDelayMs: undefined, idleTimeMs: undefined };
}

export const AutoScroll: MessageFns<AutoScroll> = {
  encode(message: AutoScroll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxSteps !== undefined) {
      writer.uint32(8).int32(message.maxSteps);
    }
    if (message.stepDelayMs !== undefined) {
      writer.uint32(16).int32(message.stepDelayMs);
    }
    if (message.idleTimeMs !== undefined) {
      writer.uint32(24).int32(message.idleTimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoScroll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAutoScroll();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.maxSteps = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.stepDelayMs = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.idleTimeMs = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AutoScroll {
    return {
      maxSteps: isSet(object.maxSteps)
        ? globalThis.Number(object.maxSteps)
        : isSet(object.max_steps)
        ? globalThis.Number(object.max_steps)
        : undefined,
      stepDelayMs: isSet(object.stepDelayMs)
        ? globalThis.Number(object.stepDelayMs)
        : isSet(object.step_delay_ms)
        ? globalThis.Number(object.step_delay_ms)
        : undefined,
      idleTimeMs: isSet(object.idleTimeMs)
        ? globalThis.Number(object.idleTimeMs)
        : isSet(object.idle_time_ms)
        ? globalThis.Number(object.idle_time_ms)
        : undefined,
    };
  },

  toJSON(message: AutoScroll): unknown {
    const obj: any = {};
    if (message.maxSteps !== undefined) {
      obj.maxSteps = Math.round(message.maxSteps);
    }
    if (message.stepDelayMs !== undefined) {
      obj.stepDelayMs = Math.round(message.stepDelayMs);
    }
    if (message.idleTimeMs !== undefined) {
      obj.idleTimeMs = Math.round(message.idleTimeMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AutoScroll>, I>>(base?: I): AutoScroll {
    return AutoScroll.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AutoScroll>, I>>(object: I): AutoScroll {
    const message = createBaseAutoScroll();
    message.maxSteps = object.maxSteps ?? undefined;
    message.stepDelayMs = object.stepDelayMs ?? undefined;
    message.idleTimeMs = object.idleTimeMs ?? undefined;
    return message;
  },
};

function createBaseAutoFetch(): AutoFetch {
  return { maxUrls: undefined };
}

export const AutoFetch: MessageFns<AutoFetch> = {
  encode(message: AutoFetch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxUrls !== undefined) {
      writer.uint32(8).int32(message.maxUrls);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoFetch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAutoFetch();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.maxUrls = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AutoFetch {
    return {
      maxUrls: isSet(object.maxUrls)
        ? globalThis.Number(object.maxUrls)
        : isSet(object.max_urls)
        ? globalThis.Number(object.max_urls)
        : undefined,
    };
  },

  toJSON(message: AutoFetch): unknown {
    const obj: any = {};
    if (message.maxUrls !== undefined) {
      obj.maxUrls = Math.round(message.maxUrls);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AutoFetch>, I>>(base?: I): AutoFetch {
    return AutoFetch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AutoFetch>, I>>(object: I): AutoFetch {
    const message = createBaseAutoFetch();
    message.maxUrls = object.maxUrls ?? undefined;
    return message;
  },
};

function createBaseAutoPlay(): AutoPlay {
  return { settleMs: undefined };
}

export const AutoPlay: MessageFns<AutoPlay> = {
  encode(message: AutoPlay, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settleMs !== undefined) {
      writer.uint32(8).int32(message.settleMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoPlay {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAutoPlay();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.settleMs = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AutoPlay {
    return {
      settleMs: isSet(object.settleMs)
        ? globalThis.Number(object.settleMs)
        : isSet(object.settle_ms)
        ? globalThis.Number(object.settle_ms)
        : undefined,
    };
  },

  toJSON(message: AutoPlay): unknown {
    const obj: any = {};
    if (message.settleMs !== undefined) {
      obj.settleMs = Math.round(message.settleMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AutoPlay>, I>>(base?: I): AutoPlay {
    return AutoPlay.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AutoPlay>, I>>(object: I): AutoPlay {
    const message = createBaseAutoPlay();
    message.settleMs = object.settleMs ?? undefined;
    return message;
  },
};

function createBaseCustom(): Custom {
  return { id: "", source: "", optionsJson: undefined };
}

export const Custom: MessageFns<Custom> = {
  encode(message: Custom, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.source !== "") {
      writer.uint32(18).string(message.source);
    }
    if (message.optionsJson !== undefined) {
      writer.uint32(26).string(message.optionsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Custom {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCustom();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.source = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.optionsJson = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Custom {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      optionsJson: isSet(object.optionsJson)
        ? globalThis.String(object.optionsJson)
        : isSet(object.options_json)
        ? globalThis.String(object.options_json)
        : undefined,
    };
  },

  toJSON(message: Custom): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.optionsJson !== undefined) {
      obj.optionsJson = message.optionsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Custom>, I>>(base?: I): Custom {
    return Custom.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Custom>, I>>(object: I): Custom {
    const message = createBaseCustom();
    message.id = object.id ?? "";
    message.source = object.source ?? "";
    message.optionsJson = object.optionsJson ?? undefined;
    return message;
  },
};

function createBaseBehaviorList(): BehaviorList {
  return { items: [] };
}

export const BehaviorList: MessageFns<BehaviorList> = {
  encode(message: BehaviorList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Behavior.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BehaviorList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBehaviorList();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.items.push(Behavior.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BehaviorList {
    return { items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Behavior.fromJSON(e)) : [] };
  },

  toJSON(message: BehaviorList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Behavior.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BehaviorList>, I>>(base?: I): BehaviorList {
    return BehaviorList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BehaviorList>, I>>(object: I): BehaviorList {
    const message = createBaseBehaviorList();
    message.items = object.items?.map((e) => Behavior.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBehaviorSpec(): BehaviorSpec {
  return { behaviors: undefined, siteBehaviors: undefined };
}

export const BehaviorSpec: MessageFns<BehaviorSpec> = {
  encode(message: BehaviorSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.behaviors !== undefined) {
      BehaviorList.encode(message.behaviors, writer.uint32(42).fork()).join();
    }
    if (message.siteBehaviors !== undefined) {
      writer.uint32(24).bool(message.siteBehaviors);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BehaviorSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBehaviorSpec();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.behaviors = BehaviorList.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.siteBehaviors = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BehaviorSpec {
    return {
      behaviors: isSet(object.behaviors) ? BehaviorList.fromJSON(object.behaviors) : undefined,
      siteBehaviors: isSet(object.siteBehaviors)
        ? globalThis.Boolean(object.siteBehaviors)
        : isSet(object.site_behaviors)
        ? globalThis.Boolean(object.site_behaviors)
        : undefined,
    };
  },

  toJSON(message: BehaviorSpec): unknown {
    const obj: any = {};
    if (message.behaviors !== undefined) {
      obj.behaviors = BehaviorList.toJSON(message.behaviors);
    }
    if (message.siteBehaviors !== undefined) {
      obj.siteBehaviors = message.siteBehaviors;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BehaviorSpec>, I>>(base?: I): BehaviorSpec {
    return BehaviorSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BehaviorSpec>, I>>(object: I): BehaviorSpec {
    const message = createBaseBehaviorSpec();
    message.behaviors = (object.behaviors !== undefined && object.behaviors !== null)
      ? BehaviorList.fromPartial(object.behaviors)
      : undefined;
    message.siteBehaviors = object.siteBehaviors ?? undefined;
    return message;
  },
};

function createBaseSubmitCaptureRequest(): SubmitCaptureRequest {
  return {
    url: "",
    labels: [],
    correlationId: undefined,
    captureFormats: undefined,
    acceptLanguage: undefined,
    signing: undefined,
    dismissBannersEnabled: undefined,
    dismissBannersSpec: undefined,
    viewport: undefined,
    operationDelayMs: undefined,
    trace: undefined,
    devicePixelRatios: [],
    fullPage: undefined,
    behaviors: undefined,
    session: 0,
    maxResponseBytes: undefined,
  };
}

export const SubmitCaptureRequest: MessageFns<SubmitCaptureRequest> = {
  encode(message: SubmitCaptureRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(18).string(v!);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.captureFormats !== undefined) {
      CaptureFormats.encode(message.captureFormats, writer.uint32(34).fork()).join();
    }
    if (message.acceptLanguage !== undefined) {
      writer.uint32(42).string(message.acceptLanguage);
    }
    if (message.signing !== undefined) {
      writer.uint32(48).bool(message.signing);
    }
    if (message.dismissBannersEnabled !== undefined) {
      writer.uint32(56).bool(message.dismissBannersEnabled);
    }
    if (message.dismissBannersSpec !== undefined) {
      DismissSpec.encode(message.dismissBannersSpec, writer.uint32(66).fork()).join();
    }
    if (message.viewport !== undefined) {
      Viewport.encode(message.viewport, writer.uint32(74).fork()).join();
    }
    if (message.operationDelayMs !== undefined) {
      writer.uint32(80).int32(message.operationDelayMs);
    }
    if (message.trace !== undefined) {
      writer.uint32(88).bool(message.trace);
    }
    writer.uint32(162).fork();
    for (const v of message.devicePixelRatios) {
      writer.int32(v);
    }
    writer.join();
    if (message.fullPage !== undefined) {
      writer.uint32(120).bool(message.fullPage);
    }
    if (message.behaviors !== undefined) {
      BehaviorSpec.encode(message.behaviors, writer.uint32(130).fork()).join();
    }
    if (message.session !== 0) {
      writer.uint32(168).int32(message.session);
    }
    if (message.maxResponseBytes !== undefined) {
      writer.uint32(152).int64(message.maxResponseBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitCaptureRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSubmitCaptureRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.captureFormats = CaptureFormats.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.acceptLanguage = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.signing = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.dismissBannersEnabled = reader.bool();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.dismissBannersSpec = DismissSpec.decode(reader, reader.uint32());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.viewport = Viewport.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.operationDelayMs = reader.int32();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }

            message.trace = reader.bool();
            continue;
          }
          case 20: {
            if (tag === 160) {
              message.devicePixelRatios.push(reader.int32());

              continue;
            }

            if (tag === 162) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.devicePixelRatios.push(reader.int32());
              }

              continue;
            }

            break;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }

            message.fullPage = reader.bool();
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.behaviors = BehaviorSpec.decode(reader, reader.uint32());
            continue;
          }
          case 21: {
            if (tag !== 168) {
              break;
            }

            message.session = reader.int32() as any;
            continue;
          }
          case 19: {
            if (tag !== 152) {
              break;
            }

            message.maxResponseBytes = longToNumber(reader.int64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SubmitCaptureRequest {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
      captureFormats: isSet(object.captureFormats)
        ? CaptureFormats.fromJSON(object.captureFormats)
        : isSet(object.capture_formats)
        ? CaptureFormats.fromJSON(object.capture_formats)
        : undefined,
      acceptLanguage: isSet(object.acceptLanguage)
        ? globalThis.String(object.acceptLanguage)
        : isSet(object.accept_language)
        ? globalThis.String(object.accept_language)
        : undefined,
      signing: isSet(object.signing) ? globalThis.Boolean(object.signing) : undefined,
      dismissBannersEnabled: isSet(object.dismissBannersEnabled)
        ? globalThis.Boolean(object.dismissBannersEnabled)
        : isSet(object.dismiss_banners_enabled)
        ? globalThis.Boolean(object.dismiss_banners_enabled)
        : undefined,
      dismissBannersSpec: isSet(object.dismissBannersSpec)
        ? DismissSpec.fromJSON(object.dismissBannersSpec)
        : isSet(object.dismiss_banners_spec)
        ? DismissSpec.fromJSON(object.dismiss_banners_spec)
        : undefined,
      viewport: isSet(object.viewport) ? Viewport.fromJSON(object.viewport) : undefined,
      operationDelayMs: isSet(object.operationDelayMs)
        ? globalThis.Number(object.operationDelayMs)
        : isSet(object.operation_delay_ms)
        ? globalThis.Number(object.operation_delay_ms)
        : undefined,
      trace: isSet(object.trace) ? globalThis.Boolean(object.trace) : undefined,
      devicePixelRatios: globalThis.Array.isArray(object?.devicePixelRatios)
        ? object.devicePixelRatios.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.device_pixel_ratios)
        ? object.device_pixel_ratios.map((e: any) => globalThis.Number(e))
        : [],
      fullPage: isSet(object.fullPage)
        ? globalThis.Boolean(object.fullPage)
        : isSet(object.full_page)
        ? globalThis.Boolean(object.full_page)
        : undefined,
      behaviors: isSet(object.behaviors) ? BehaviorSpec.fromJSON(object.behaviors) : undefined,
      session: isSet(object.session) ? sessionModeFromJSON(object.session) : 0,
      maxResponseBytes: isSet(object.maxResponseBytes)
        ? globalThis.Number(object.maxResponseBytes)
        : isSet(object.max_response_bytes)
        ? globalThis.Number(object.max_response_bytes)
        : undefined,
    };
  },

  toJSON(message: SubmitCaptureRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    if (message.captureFormats !== undefined) {
      obj.captureFormats = CaptureFormats.toJSON(message.captureFormats);
    }
    if (message.acceptLanguage !== undefined) {
      obj.acceptLanguage = message.acceptLanguage;
    }
    if (message.signing !== undefined) {
      obj.signing = message.signing;
    }
    if (message.dismissBannersEnabled !== undefined) {
      obj.dismissBannersEnabled = message.dismissBannersEnabled;
    }
    if (message.dismissBannersSpec !== undefined) {
      obj.dismissBannersSpec = DismissSpec.toJSON(message.dismissBannersSpec);
    }
    if (message.viewport !== undefined) {
      obj.viewport = Viewport.toJSON(message.viewport);
    }
    if (message.operationDelayMs !== undefined) {
      obj.operationDelayMs = Math.round(message.operationDelayMs);
    }
    if (message.trace !== undefined) {
      obj.trace = message.trace;
    }
    if (message.devicePixelRatios?.length) {
      obj.devicePixelRatios = message.devicePixelRatios.map((e) => Math.round(e));
    }
    if (message.fullPage !== undefined) {
      obj.fullPage = message.fullPage;
    }
    if (message.behaviors !== undefined) {
      obj.behaviors = BehaviorSpec.toJSON(message.behaviors);
    }
    if (message.session !== 0) {
      obj.session = sessionModeToJSON(message.session);
    }
    if (message.maxResponseBytes !== undefined) {
      obj.maxResponseBytes = Math.round(message.maxResponseBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitCaptureRequest>, I>>(base?: I): SubmitCaptureRequest {
    return SubmitCaptureRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitCaptureRequest>, I>>(object: I): SubmitCaptureRequest {
    const message = createBaseSubmitCaptureRequest();
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.correlationId = object.correlationId ?? undefined;
    message.captureFormats = (object.captureFormats !== undefined && object.captureFormats !== null)
      ? CaptureFormats.fromPartial(object.captureFormats)
      : undefined;
    message.acceptLanguage = object.acceptLanguage ?? undefined;
    message.signing = object.signing ?? undefined;
    message.dismissBannersEnabled = object.dismissBannersEnabled ?? undefined;
    message.dismissBannersSpec = (object.dismissBannersSpec !== undefined && object.dismissBannersSpec !== null)
      ? DismissSpec.fromPartial(object.dismissBannersSpec)
      : undefined;
    message.viewport = (object.viewport !== undefined && object.viewport !== null)
      ? Viewport.fromPartial(object.viewport)
      : undefined;
    message.operationDelayMs = object.operationDelayMs ?? undefined;
    message.trace = object.trace ?? undefined;
    message.devicePixelRatios = object.devicePixelRatios?.map((e) => e) || [];
    message.fullPage = object.fullPage ?? undefined;
    message.behaviors = (object.behaviors !== undefined && object.behaviors !== null)
      ? BehaviorSpec.fromPartial(object.behaviors)
      : undefined;
    message.session = object.session ?? 0;
    message.maxResponseBytes = object.maxResponseBytes ?? undefined;
    return message;
  },
};

function createBaseGetCaptureRequest(): GetCaptureRequest {
  return { taskId: "" };
}

export const GetCaptureRequest: MessageFns<GetCaptureRequest> = {
  encode(message: GetCaptureRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCaptureRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetCaptureRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetCaptureRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: GetCaptureRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetCaptureRequest>, I>>(base?: I): GetCaptureRequest {
    return GetCaptureRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetCaptureRequest>, I>>(object: I): GetCaptureRequest {
    const message = createBaseGetCaptureRequest();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

function createBaseGetStatusRequest(): GetStatusRequest {
  return { pendingLimit: undefined };
}

export const GetStatusRequest: MessageFns<GetStatusRequest> = {
  encode(message: GetStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pendingLimit !== undefined) {
      writer.uint32(8).int32(message.pendingLimit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetStatusRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.pendingLimit = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetStatusRequest {
    return {
      pendingLimit: isSet(object.pendingLimit)
        ? globalThis.Number(object.pendingLimit)
        : isSet(object.pending_limit)
        ? globalThis.Number(object.pending_limit)
        : undefined,
    };
  },

  toJSON(message: GetStatusRequest): unknown {
    const obj: any = {};
    if (message.pendingLimit !== undefined) {
      obj.pendingLimit = Math.round(message.pendingLimit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStatusRequest>, I>>(base?: I): GetStatusRequest {
    return GetStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStatusRequest>, I>>(object: I): GetStatusRequest {
    const message = createBaseGetStatusRequest();
    message.pendingLimit = object.pendingLimit ?? undefined;
    return message;
  },
};

function createBaseSubmitCaptureResponse(): SubmitCaptureResponse {
  return { accepted: false, taskId: "", correlationId: undefined };
}

export const SubmitCaptureResponse: MessageFns<SubmitCaptureResponse> = {
  encode(message: SubmitCaptureResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.taskId !== "") {
      writer.uint32(18).string(message.taskId);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(26).string(message.correlationId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitCaptureResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSubmitCaptureResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.accepted = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SubmitCaptureResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
    };
  },

  toJSON(message: SubmitCaptureResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitCaptureResponse>, I>>(base?: I): SubmitCaptureResponse {
    return SubmitCaptureResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitCaptureResponse>, I>>(object: I): SubmitCaptureResponse {
    const message = createBaseSubmitCaptureResponse();
    message.accepted = object.accepted ?? false;
    message.taskId = object.taskId ?? "";
    message.correlationId = object.correlationId ?? undefined;
    return message;
  },
};

function createBaseCaptureArtifacts(): CaptureArtifacts {
  return { png: undefined, webp: undefined, html: undefined, links: undefined, mhtml: undefined, wacz: undefined };
}

export const CaptureArtifacts: MessageFns<CaptureArtifacts> = {
  encode(message: CaptureArtifacts, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.png !== undefined) {
      writer.uint32(10).string(message.png);
    }
    if (message.webp !== undefined) {
      writer.uint32(18).string(message.webp);
    }
    if (message.html !== undefined) {
      writer.uint32(26).string(message.html);
    }
    if (message.links !== undefined) {
      writer.uint32(34).string(message.links);
    }
    if (message.mhtml !== undefined) {
      writer.uint32(42).string(message.mhtml);
    }
    if (message.wacz !== undefined) {
      writer.uint32(50).string(message.wacz);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureArtifacts {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCaptureArtifacts();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.png = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.webp = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.html = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.links = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.mhtml = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.wacz = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CaptureArtifacts {
    return {
      png: isSet(object.png) ? globalThis.String(object.png) : undefined,
      webp: isSet(object.webp) ? globalThis.String(object.webp) : undefined,
      html: isSet(object.html) ? globalThis.String(object.html) : undefined,
      links: isSet(object.links) ? globalThis.String(object.links) : undefined,
      mhtml: isSet(object.mhtml) ? globalThis.String(object.mhtml) : undefined,
      wacz: isSet(object.wacz) ? globalThis.String(object.wacz) : undefined,
    };
  },

  toJSON(message: CaptureArtifacts): unknown {
    const obj: any = {};
    if (message.png !== undefined) {
      obj.png = message.png;
    }
    if (message.webp !== undefined) {
      obj.webp = message.webp;
    }
    if (message.html !== undefined) {
      obj.html = message.html;
    }
    if (message.links !== undefined) {
      obj.links = message.links;
    }
    if (message.mhtml !== undefined) {
      obj.mhtml = message.mhtml;
    }
    if (message.wacz !== undefined) {
      obj.wacz = message.wacz;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureArtifacts>, I>>(base?: I): CaptureArtifacts {
    return CaptureArtifacts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureArtifacts>, I>>(object: I): CaptureArtifacts {
    const message = createBaseCaptureArtifacts();
    message.png = object.png ?? undefined;
    message.webp = object.webp ?? undefined;
    message.html = object.html ?? undefined;
    message.links = object.links ?? undefined;
    message.mhtml = object.mhtml ?? undefined;
    message.wacz = object.wacz ?? undefined;
    return message;
  },
};

function createBaseWaczStats(): WaczStats {
  return {
    totalRecorded: 0,
    totalBlocked: 0,
    totalSkippedContentType: 0,
    totalTruncatedTooLarge: 0,
    totalTruncatedTaskCap: 0,
    totalFailed: 0,
    totalIncomplete: 0,
    totalBodyBytes: 0,
  };
}

export const WaczStats: MessageFns<WaczStats> = {
  encode(message: WaczStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalRecorded !== 0) {
      writer.uint32(8).int32(message.totalRecorded);
    }
    if (message.totalBlocked !== 0) {
      writer.uint32(16).int32(message.totalBlocked);
    }
    if (message.totalSkippedContentType !== 0) {
      writer.uint32(24).int32(message.totalSkippedContentType);
    }
    if (message.totalTruncatedTooLarge !== 0) {
      writer.uint32(32).int32(message.totalTruncatedTooLarge);
    }
    if (message.totalTruncatedTaskCap !== 0) {
      writer.uint32(40).int32(message.totalTruncatedTaskCap);
    }
    if (message.totalFailed !== 0) {
      writer.uint32(48).int32(message.totalFailed);
    }
    if (message.totalIncomplete !== 0) {
      writer.uint32(56).int32(message.totalIncomplete);
    }
    if (message.totalBodyBytes !== 0) {
      writer.uint32(64).int64(message.totalBodyBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaczStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWaczStats();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.totalRecorded = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.totalBlocked = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.totalSkippedContentType = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.totalTruncatedTooLarge = reader.int32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.totalTruncatedTaskCap = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.totalFailed = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.totalIncomplete = reader.int32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.totalBodyBytes = longToNumber(reader.int64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): WaczStats {
    return {
      totalRecorded: isSet(object.totalRecorded)
        ? globalThis.Number(object.totalRecorded)
        : isSet(object.total_recorded)
        ? globalThis.Number(object.total_recorded)
        : 0,
      totalBlocked: isSet(object.totalBlocked)
        ? globalThis.Number(object.totalBlocked)
        : isSet(object.total_blocked)
        ? globalThis.Number(object.total_blocked)
        : 0,
      totalSkippedContentType: isSet(object.totalSkippedContentType)
        ? globalThis.Number(object.totalSkippedContentType)
        : isSet(object.total_skipped_content_type)
        ? globalThis.Number(object.total_skipped_content_type)
        : 0,
      totalTruncatedTooLarge: isSet(object.totalTruncatedTooLarge)
        ? globalThis.Number(object.totalTruncatedTooLarge)
        : isSet(object.total_truncated_too_large)
        ? globalThis.Number(object.total_truncated_too_large)
        : 0,
      totalTruncatedTaskCap: isSet(object.totalTruncatedTaskCap)
        ? globalThis.Number(object.totalTruncatedTaskCap)
        : isSet(object.total_truncated_task_cap)
        ? globalThis.Number(object.total_truncated_task_cap)
        : 0,
      totalFailed: isSet(object.totalFailed)
        ? globalThis.Number(object.totalFailed)
        : isSet(object.total_failed)
        ? globalThis.Number(object.total_failed)
        : 0,
      totalIncomplete: isSet(object.totalIncomplete)
        ? globalThis.Number(object.totalIncomplete)
        : isSet(object.total_incomplete)
        ? globalThis.Number(object.total_incomplete)
        : 0,
      totalBodyBytes: isSet(object.totalBodyBytes)
        ? globalThis.Number(object.totalBodyBytes)
        : isSet(object.total_body_bytes)
        ? globalThis.Number(object.total_body_bytes)
        : 0,
    };
  },

  toJSON(message: WaczStats): unknown {
    const obj: any = {};
    if (message.totalRecorded !== 0) {
      obj.totalRecorded = Math.round(message.totalRecorded);
    }
    if (message.totalBlocked !== 0) {
      obj.totalBlocked = Math.round(message.totalBlocked);
    }
    if (message.totalSkippedContentType !== 0) {
      obj.totalSkippedContentType = Math.round(message.totalSkippedContentType);
    }
    if (message.totalTruncatedTooLarge !== 0) {
      obj.totalTruncatedTooLarge = Math.round(message.totalTruncatedTooLarge);
    }
    if (message.totalTruncatedTaskCap !== 0) {
      obj.totalTruncatedTaskCap = Math.round(message.totalTruncatedTaskCap);
    }
    if (message.totalFailed !== 0) {
      obj.totalFailed = Math.round(message.totalFailed);
    }
    if (message.totalIncomplete !== 0) {
      obj.totalIncomplete = Math.round(message.totalIncomplete);
    }
    if (message.totalBodyBytes !== 0) {
      obj.totalBodyBytes = Math.round(message.totalBodyBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaczStats>, I>>(base?: I): WaczStats {
    return WaczStats.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaczStats>, I>>(object: I): WaczStats {
    const message = createBaseWaczStats();
    message.totalRecorded = object.totalRecorded ?? 0;
    message.totalBlocked = object.totalBlocked ?? 0;
    message.totalSkippedContentType = object.totalSkippedContentType ?? 0;
    message.totalTruncatedTooLarge = object.totalTruncatedTooLarge ?? 0;
    message.totalTruncatedTaskCap = object.totalTruncatedTaskCap ?? 0;
    message.totalFailed = object.totalFailed ?? 0;
    message.totalIncomplete = object.totalIncomplete ?? 0;
    message.totalBodyBytes = object.totalBodyBytes ?? 0;
    return message;
  },
};

function createBaseWaczSignatureChecks(): WaczSignatureChecks {
  return { signature: 0, chain: 0, domain: 0, timestamp: 0 };
}

export const WaczSignatureChecks: MessageFns<WaczSignatureChecks> = {
  encode(message: WaczSignatureChecks, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signature !== 0) {
      writer.uint32(8).int32(message.signature);
    }
    if (message.chain !== 0) {
      writer.uint32(16).int32(message.chain);
    }
    if (message.domain !== 0) {
      writer.uint32(24).int32(message.domain);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int32(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaczSignatureChecks {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWaczSignatureChecks();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.signature = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.chain = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.domain = reader.int32() as any;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.timestamp = reader.int32() as any;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): WaczSignatureChecks {
    return {
      signature: isSet(object.signature) ? checkOutcomeFromJSON(object.signature) : 0,
      chain: isSet(object.chain) ? checkOutcomeFromJSON(object.chain) : 0,
      domain: isSet(object.domain) ? checkOutcomeFromJSON(object.domain) : 0,
      timestamp: isSet(object.timestamp) ? checkOutcomeFromJSON(object.timestamp) : 0,
    };
  },

  toJSON(message: WaczSignatureChecks): unknown {
    const obj: any = {};
    if (message.signature !== 0) {
      obj.signature = checkOutcomeToJSON(message.signature);
    }
    if (message.chain !== 0) {
      obj.chain = checkOutcomeToJSON(message.chain);
    }
    if (message.domain !== 0) {
      obj.domain = checkOutcomeToJSON(message.domain);
    }
    if (message.timestamp !== 0) {
      obj.timestamp = checkOutcomeToJSON(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaczSignatureChecks>, I>>(base?: I): WaczSignatureChecks {
    return WaczSignatureChecks.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaczSignatureChecks>, I>>(object: I): WaczSignatureChecks {
    const message = createBaseWaczSignatureChecks();
    message.signature = object.signature ?? 0;
    message.chain = object.chain ?? 0;
    message.domain = object.domain ?? 0;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseWaczSignature(): WaczSignature {
  return { signed: false, reason: undefined, domain: undefined, checks: undefined };
}

export const WaczSignature: MessageFns<WaczSignature> = {
  encode(message: WaczSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signed !== false) {
      writer.uint32(8).bool(message.signed);
    }
    if (message.reason !== undefined) {
      writer.uint32(18).string(message.reason);
    }
    if (message.domain !== undefined) {
      writer.uint32(26).string(message.domain);
    }
    if (message.checks !== undefined) {
      WaczSignatureChecks.encode(message.checks, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaczSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWaczSignature();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.signed = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.reason = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.domain = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.checks = WaczSignatureChecks.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): WaczSignature {
    return {
      signed: isSet(object.signed) ? globalThis.Boolean(object.signed) : false,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : undefined,
      checks: isSet(object.checks) ? WaczSignatureChecks.fromJSON(object.checks) : undefined,
    };
  },

  toJSON(message: WaczSignature): unknown {
    const obj: any = {};
    if (message.signed !== false) {
      obj.signed = message.signed;
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    if (message.domain !== undefined) {
      obj.domain = message.domain;
    }
    if (message.checks !== undefined) {
      obj.checks = WaczSignatureChecks.toJSON(message.checks);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaczSignature>, I>>(base?: I): WaczSignature {
    return WaczSignature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaczSignature>, I>>(object: I): WaczSignature {
    const message = createBaseWaczSignature();
    message.signed = object.signed ?? false;
    message.reason = object.reason ?? undefined;
    message.domain = object.domain ?? undefined;
    message.checks = (object.checks !== undefined && object.checks !== null)
      ? WaczSignatureChecks.fromPartial(object.checks)
      : undefined;
    return message;
  },
};

function createBaseWaczCompleteness(): WaczCompleteness {
  return { bodylessUrls: [], truncatedUrls: [], complete: false };
}

export const WaczCompleteness: MessageFns<WaczCompleteness> = {
  encode(message: WaczCompleteness, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.bodylessUrls) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.truncatedUrls) {
      writer.uint32(18).string(v!);
    }
    if (message.complete !== false) {
      writer.uint32(24).bool(message.complete);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaczCompleteness {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWaczCompleteness();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.bodylessUrls.push(reader.string());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.truncatedUrls.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.complete = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): WaczCompleteness {
    return {
      bodylessUrls: globalThis.Array.isArray(object?.bodylessUrls)
        ? object.bodylessUrls.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.bodyless_urls)
        ? object.bodyless_urls.map((e: any) => globalThis.String(e))
        : [],
      truncatedUrls: globalThis.Array.isArray(object?.truncatedUrls)
        ? object.truncatedUrls.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.truncated_urls)
        ? object.truncated_urls.map((e: any) => globalThis.String(e))
        : [],
      complete: isSet(object.complete) ? globalThis.Boolean(object.complete) : false,
    };
  },

  toJSON(message: WaczCompleteness): unknown {
    const obj: any = {};
    if (message.bodylessUrls?.length) {
      obj.bodylessUrls = message.bodylessUrls;
    }
    if (message.truncatedUrls?.length) {
      obj.truncatedUrls = message.truncatedUrls;
    }
    if (message.complete !== false) {
      obj.complete = message.complete;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaczCompleteness>, I>>(base?: I): WaczCompleteness {
    return WaczCompleteness.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaczCompleteness>, I>>(object: I): WaczCompleteness {
    const message = createBaseWaczCompleteness();
    message.bodylessUrls = object.bodylessUrls?.map((e) => e) || [];
    message.truncatedUrls = object.truncatedUrls?.map((e) => e) || [];
    message.complete = object.complete ?? false;
    return message;
  },
};

function createBaseCaptureErrorDetails(): CaptureErrorDetails {
  return { type: 0, message: "", httpStatusCode: undefined, httpStatusText: undefined, timeoutMs: undefined };
}

export const CaptureErrorDetails: MessageFns<CaptureErrorDetails> = {
  encode(message: CaptureErrorDetails, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.httpStatusCode !== undefined) {
      writer.uint32(24).int32(message.httpStatusCode);
    }
    if (message.httpStatusText !== undefined) {
      writer.uint32(34).string(message.httpStatusText);
    }
    if (message.timeoutMs !== undefined) {
      writer.uint32(40).int32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureErrorDetails {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCaptureErrorDetails();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.httpStatusCode = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.httpStatusText = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.timeoutMs = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CaptureErrorDetails {
    return {
      type: isSet(object.type) ? errorTypeFromJSON(object.type) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      httpStatusCode: isSet(object.httpStatusCode)
        ? globalThis.Number(object.httpStatusCode)
        : isSet(object.http_status_code)
        ? globalThis.Number(object.http_status_code)
        : undefined,
      httpStatusText: isSet(object.httpStatusText)
        ? globalThis.String(object.httpStatusText)
        : isSet(object.http_status_text)
        ? globalThis.String(object.http_status_text)
        : undefined,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : undefined,
    };
  },

  toJSON(message: CaptureErrorDetails): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = errorTypeToJSON(message.type);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.httpStatusCode !== undefined) {
      obj.httpStatusCode = Math.round(message.httpStatusCode);
    }
    if (message.httpStatusText !== undefined) {
      obj.httpStatusText = message.httpStatusText;
    }
    if (message.timeoutMs !== undefined) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureErrorDetails>, I>>(base?: I): CaptureErrorDetails {
    return CaptureErrorDetails.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureErrorDetails>, I>>(object: I): CaptureErrorDetails {
    const message = createBaseCaptureErrorDetails();
    message.type = object.type ?? 0;
    message.message = object.message ?? "";
    message.httpStatusCode = object.httpStatusCode ?? undefined;
    message.httpStatusText = object.httpStatusText ?? undefined;
    message.timeoutMs = object.timeoutMs ?? undefined;
    return message;
  },
};

function createBaseCaptureResultReport(): CaptureResultReport {
  return {
    taskId: "",
    correlationId: undefined,
    url: "",
    labels: [],
    status: 0,
    httpStatusCode: undefined,
    timestamp: "",
    captureProcessingTimeMs: 0,
    retryCount: 0,
    workerIndex: 0,
    artifacts: undefined,
    waczStats: undefined,
    completeness: undefined,
    signature: undefined,
    errorDetails: undefined,
  };
}

export const CaptureResultReport: MessageFns<CaptureResultReport> = {
  encode(message: CaptureResultReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(18).string(message.correlationId);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(34).string(v!);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.httpStatusCode !== undefined) {
      writer.uint32(48).int32(message.httpStatusCode);
    }
    if (message.timestamp !== "") {
      writer.uint32(58).string(message.timestamp);
    }
    if (message.captureProcessingTimeMs !== 0) {
      writer.uint32(64).int32(message.captureProcessingTimeMs);
    }
    if (message.retryCount !== 0) {
      writer.uint32(72).int32(message.retryCount);
    }
    if (message.workerIndex !== 0) {
      writer.uint32(80).int32(message.workerIndex);
    }
    if (message.artifacts !== undefined) {
      CaptureArtifacts.encode(message.artifacts, writer.uint32(90).fork()).join();
    }
    if (message.waczStats !== undefined) {
      WaczStats.encode(message.waczStats, writer.uint32(98).fork()).join();
    }
    if (message.completeness !== undefined) {
      WaczCompleteness.encode(message.completeness, writer.uint32(106).fork()).join();
    }
    if (message.signature !== undefined) {
      WaczSignature.encode(message.signature, writer.uint32(114).fork()).join();
    }
    if (message.errorDetails !== undefined) {
      CaptureErrorDetails.encode(message.errorDetails, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureResultReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCaptureResultReport();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.httpStatusCode = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.timestamp = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.captureProcessingTimeMs = reader.int32();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.retryCount = reader.int32();
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.workerIndex = reader.int32();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.artifacts = CaptureArtifacts.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.waczStats = WaczStats.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.completeness = WaczCompleteness.decode(reader, reader.uint32());
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.signature = WaczSignature.decode(reader, reader.uint32());
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.errorDetails = CaptureErrorDetails.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CaptureResultReport {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      status: isSet(object.status) ? captureStatusFromJSON(object.status) : 0,
      httpStatusCode: isSet(object.httpStatusCode)
        ? globalThis.Number(object.httpStatusCode)
        : isSet(object.http_status_code)
        ? globalThis.Number(object.http_status_code)
        : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
      captureProcessingTimeMs: isSet(object.captureProcessingTimeMs)
        ? globalThis.Number(object.captureProcessingTimeMs)
        : isSet(object.capture_processing_time_ms)
        ? globalThis.Number(object.capture_processing_time_ms)
        : 0,
      retryCount: isSet(object.retryCount)
        ? globalThis.Number(object.retryCount)
        : isSet(object.retry_count)
        ? globalThis.Number(object.retry_count)
        : 0,
      workerIndex: isSet(object.workerIndex)
        ? globalThis.Number(object.workerIndex)
        : isSet(object.worker_index)
        ? globalThis.Number(object.worker_index)
        : 0,
      artifacts: isSet(object.artifacts) ? CaptureArtifacts.fromJSON(object.artifacts) : undefined,
      waczStats: isSet(object.waczStats)
        ? WaczStats.fromJSON(object.waczStats)
        : isSet(object.wacz_stats)
        ? WaczStats.fromJSON(object.wacz_stats)
        : undefined,
      completeness: isSet(object.completeness) ? WaczCompleteness.fromJSON(object.completeness) : undefined,
      signature: isSet(object.signature) ? WaczSignature.fromJSON(object.signature) : undefined,
      errorDetails: isSet(object.errorDetails)
        ? CaptureErrorDetails.fromJSON(object.errorDetails)
        : isSet(object.error_details)
        ? CaptureErrorDetails.fromJSON(object.error_details)
        : undefined,
    };
  },

  toJSON(message: CaptureResultReport): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.status !== 0) {
      obj.status = captureStatusToJSON(message.status);
    }
    if (message.httpStatusCode !== undefined) {
      obj.httpStatusCode = Math.round(message.httpStatusCode);
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.captureProcessingTimeMs !== 0) {
      obj.captureProcessingTimeMs = Math.round(message.captureProcessingTimeMs);
    }
    if (message.retryCount !== 0) {
      obj.retryCount = Math.round(message.retryCount);
    }
    if (message.workerIndex !== 0) {
      obj.workerIndex = Math.round(message.workerIndex);
    }
    if (message.artifacts !== undefined) {
      obj.artifacts = CaptureArtifacts.toJSON(message.artifacts);
    }
    if (message.waczStats !== undefined) {
      obj.waczStats = WaczStats.toJSON(message.waczStats);
    }
    if (message.completeness !== undefined) {
      obj.completeness = WaczCompleteness.toJSON(message.completeness);
    }
    if (message.signature !== undefined) {
      obj.signature = WaczSignature.toJSON(message.signature);
    }
    if (message.errorDetails !== undefined) {
      obj.errorDetails = CaptureErrorDetails.toJSON(message.errorDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureResultReport>, I>>(base?: I): CaptureResultReport {
    return CaptureResultReport.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureResultReport>, I>>(object: I): CaptureResultReport {
    const message = createBaseCaptureResultReport();
    message.taskId = object.taskId ?? "";
    message.correlationId = object.correlationId ?? undefined;
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.status = object.status ?? 0;
    message.httpStatusCode = object.httpStatusCode ?? undefined;
    message.timestamp = object.timestamp ?? "";
    message.captureProcessingTimeMs = object.captureProcessingTimeMs ?? 0;
    message.retryCount = object.retryCount ?? 0;
    message.workerIndex = object.workerIndex ?? 0;
    message.artifacts = (object.artifacts !== undefined && object.artifacts !== null)
      ? CaptureArtifacts.fromPartial(object.artifacts)
      : undefined;
    message.waczStats = (object.waczStats !== undefined && object.waczStats !== null)
      ? WaczStats.fromPartial(object.waczStats)
      : undefined;
    message.completeness = (object.completeness !== undefined && object.completeness !== null)
      ? WaczCompleteness.fromPartial(object.completeness)
      : undefined;
    message.signature = (object.signature !== undefined && object.signature !== null)
      ? WaczSignature.fromPartial(object.signature)
      : undefined;
    message.errorDetails = (object.errorDetails !== undefined && object.errorDetails !== null)
      ? CaptureErrorDetails.fromPartial(object.errorDetails)
      : undefined;
    return message;
  },
};

function createBaseGetCaptureResponse(): GetCaptureResponse {
  return { state: 0, report: undefined };
}

export const GetCaptureResponse: MessageFns<GetCaptureResponse> = {
  encode(message: GetCaptureResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.report !== undefined) {
      CaptureResultReport.encode(message.report, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCaptureResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetCaptureResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.state = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.report = CaptureResultReport.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetCaptureResponse {
    return {
      state: isSet(object.state) ? captureStateFromJSON(object.state) : 0,
      report: isSet(object.report) ? CaptureResultReport.fromJSON(object.report) : undefined,
    };
  },

  toJSON(message: GetCaptureResponse): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = captureStateToJSON(message.state);
    }
    if (message.report !== undefined) {
      obj.report = CaptureResultReport.toJSON(message.report);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetCaptureResponse>, I>>(base?: I): GetCaptureResponse {
    return GetCaptureResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetCaptureResponse>, I>>(object: I): GetCaptureResponse {
    const message = createBaseGetCaptureResponse();
    message.state = object.state ?? 0;
    message.report = (object.report !== undefined && object.report !== null)
      ? CaptureResultReport.fromPartial(object.report)
      : undefined;
    return message;
  },
};

function createBaseErrorTaskInfo(): ErrorTaskInfo {
  return { taskId: "", url: "", labels: [] };
}

export const ErrorTaskInfo: MessageFns<ErrorTaskInfo> = {
  encode(message: ErrorTaskInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorTaskInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseErrorTaskInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ErrorTaskInfo {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ErrorTaskInfo): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorTaskInfo>, I>>(base?: I): ErrorTaskInfo {
    return ErrorTaskInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorTaskInfo>, I>>(object: I): ErrorTaskInfo {
    const message = createBaseErrorTaskInfo();
    message.taskId = object.taskId ?? "";
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    return message;
  },
};

function createBaseErrorRecord(): ErrorRecord {
  return {
    type: 0,
    message: "",
    httpStatusCode: undefined,
    httpStatusText: undefined,
    timeoutMs: undefined,
    timestamp: "",
    task: undefined,
  };
}

export const ErrorRecord: MessageFns<ErrorRecord> = {
  encode(message: ErrorRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.httpStatusCode !== undefined) {
      writer.uint32(24).int32(message.httpStatusCode);
    }
    if (message.httpStatusText !== undefined) {
      writer.uint32(34).string(message.httpStatusText);
    }
    if (message.timeoutMs !== undefined) {
      writer.uint32(40).int32(message.timeoutMs);
    }
    if (message.timestamp !== "") {
      writer.uint32(50).string(message.timestamp);
    }
    if (message.task !== undefined) {
      ErrorTaskInfo.encode(message.task, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseErrorRecord();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.httpStatusCode = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.httpStatusText = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.timeoutMs = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.timestamp = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.task = ErrorTaskInfo.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ErrorRecord {
    return {
      type: isSet(object.type) ? errorTypeFromJSON(object.type) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      httpStatusCode: isSet(object.httpStatusCode)
        ? globalThis.Number(object.httpStatusCode)
        : isSet(object.http_status_code)
        ? globalThis.Number(object.http_status_code)
        : undefined,
      httpStatusText: isSet(object.httpStatusText)
        ? globalThis.String(object.httpStatusText)
        : isSet(object.http_status_text)
        ? globalThis.String(object.http_status_text)
        : undefined,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
      task: isSet(object.task) ? ErrorTaskInfo.fromJSON(object.task) : undefined,
    };
  },

  toJSON(message: ErrorRecord): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = errorTypeToJSON(message.type);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.httpStatusCode !== undefined) {
      obj.httpStatusCode = Math.round(message.httpStatusCode);
    }
    if (message.httpStatusText !== undefined) {
      obj.httpStatusText = message.httpStatusText;
    }
    if (message.timeoutMs !== undefined) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.task !== undefined) {
      obj.task = ErrorTaskInfo.toJSON(message.task);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorRecord>, I>>(base?: I): ErrorRecord {
    return ErrorRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorRecord>, I>>(object: I): ErrorRecord {
    const message = createBaseErrorRecord();
    message.type = object.type ?? 0;
    message.message = object.message ?? "";
    message.httpStatusCode = object.httpStatusCode ?? undefined;
    message.httpStatusText = object.httpStatusText ?? undefined;
    message.timeoutMs = object.timeoutMs ?? undefined;
    message.timestamp = object.timestamp ?? "";
    message.task = (object.task !== undefined && object.task !== null)
      ? ErrorTaskInfo.fromPartial(object.task)
      : undefined;
    return message;
  },
};

function createBaseBrowserOptions(): BrowserOptions {
  return { browserUrl: "" };
}

export const BrowserOptions: MessageFns<BrowserOptions> = {
  encode(message: BrowserOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.browserUrl !== "") {
      writer.uint32(10).string(message.browserUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BrowserOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBrowserOptions();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.browserUrl = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BrowserOptions {
    return {
      browserUrl: isSet(object.browserUrl)
        ? globalThis.String(object.browserUrl)
        : isSet(object.browser_url)
        ? globalThis.String(object.browser_url)
        : "",
    };
  },

  toJSON(message: BrowserOptions): unknown {
    const obj: any = {};
    if (message.browserUrl !== "") {
      obj.browserUrl = message.browserUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BrowserOptions>, I>>(base?: I): BrowserOptions {
    return BrowserOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BrowserOptions>, I>>(object: I): BrowserOptions {
    const message = createBaseBrowserOptions();
    message.browserUrl = object.browserUrl ?? "";
    return message;
  },
};

function createBaseCurrentTask(): CurrentTask {
  return { taskId: "", url: "", labels: [], correlationId: undefined, startedAt: "", elapsedMs: 0, retryCount: 0 };
}

export const CurrentTask: MessageFns<CurrentTask> = {
  encode(message: CurrentTask, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(26).string(v!);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(34).string(message.correlationId);
    }
    if (message.startedAt !== "") {
      writer.uint32(42).string(message.startedAt);
    }
    if (message.elapsedMs !== 0) {
      writer.uint32(48).int32(message.elapsedMs);
    }
    if (message.retryCount !== 0) {
      writer.uint32(56).int32(message.retryCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CurrentTask {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCurrentTask();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.startedAt = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.elapsedMs = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.retryCount = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CurrentTask {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
      startedAt: isSet(object.startedAt)
        ? globalThis.String(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.String(object.started_at)
        : "",
      elapsedMs: isSet(object.elapsedMs)
        ? globalThis.Number(object.elapsedMs)
        : isSet(object.elapsed_ms)
        ? globalThis.Number(object.elapsed_ms)
        : 0,
      retryCount: isSet(object.retryCount)
        ? globalThis.Number(object.retryCount)
        : isSet(object.retry_count)
        ? globalThis.Number(object.retry_count)
        : 0,
    };
  },

  toJSON(message: CurrentTask): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    if (message.startedAt !== "") {
      obj.startedAt = message.startedAt;
    }
    if (message.elapsedMs !== 0) {
      obj.elapsedMs = Math.round(message.elapsedMs);
    }
    if (message.retryCount !== 0) {
      obj.retryCount = Math.round(message.retryCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CurrentTask>, I>>(base?: I): CurrentTask {
    return CurrentTask.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CurrentTask>, I>>(object: I): CurrentTask {
    const message = createBaseCurrentTask();
    message.taskId = object.taskId ?? "";
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.correlationId = object.correlationId ?? undefined;
    message.startedAt = object.startedAt ?? "";
    message.elapsedMs = object.elapsedMs ?? 0;
    message.retryCount = object.retryCount ?? 0;
    return message;
  },
};

function createBaseWorkerInfo(): WorkerInfo {
  return {
    index: 0,
    browserOptions: undefined,
    health: 0,
    processedCount: 0,
    errorCount: 0,
    errorHistory: [],
    currentTask: undefined,
  };
}

export const WorkerInfo: MessageFns<WorkerInfo> = {
  encode(message: WorkerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.browserOptions !== undefined) {
      BrowserOptions.encode(message.browserOptions, writer.uint32(18).fork()).join();
    }
    if (message.health !== 0) {
      writer.uint32(24).int32(message.health);
    }
    if (message.processedCount !== 0) {
      writer.uint32(32).int32(message.processedCount);
    }
    if (message.errorCount !== 0) {
      writer.uint32(40).int32(message.errorCount);
    }
    for (const v of message.errorHistory) {
      ErrorRecord.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.currentTask !== undefined) {
      CurrentTask.encode(message.currentTask, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseWorkerInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.index = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.browserOptions = BrowserOptions.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.health = reader.int32() as any;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.processedCount = reader.int32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.errorCount = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.errorHistory.push(ErrorRecord.decode(reader, reader.uint32()));
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.currentTask = CurrentTask.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): WorkerInfo {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      browserOptions: isSet(object.browserOptions)
        ? BrowserOptions.fromJSON(object.browserOptions)
        : isSet(object.browser_options)
        ? BrowserOptions.fromJSON(object.browser_options)
        : undefined,
      health: isSet(object.health) ? workerHealthFromJSON(object.health) : 0,
      processedCount: isSet(object.processedCount)
        ? globalThis.Number(object.processedCount)
        : isSet(object.processed_count)
        ? globalThis.Number(object.processed_count)
        : 0,
      errorCount: isSet(object.errorCount)
        ? globalThis.Number(object.errorCount)
        : isSet(object.error_count)
        ? globalThis.Number(object.error_count)
        : 0,
      errorHistory: globalThis.Array.isArray(object?.errorHistory)
        ? object.errorHistory.map((e: any) => ErrorRecord.fromJSON(e))
        : globalThis.Array.isArray(object?.error_history)
        ? object.error_history.map((e: any) => ErrorRecord.fromJSON(e))
        : [],
      currentTask: isSet(object.currentTask)
        ? CurrentTask.fromJSON(object.currentTask)
        : isSet(object.current_task)
        ? CurrentTask.fromJSON(object.current_task)
        : undefined,
    };
  },

  toJSON(message: WorkerInfo): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.browserOptions !== undefined) {
      obj.browserOptions = BrowserOptions.toJSON(message.browserOptions);
    }
    if (message.health !== 0) {
      obj.health = workerHealthToJSON(message.health);
    }
    if (message.processedCount !== 0) {
      obj.processedCount = Math.round(message.processedCount);
    }
    if (message.errorCount !== 0) {
      obj.errorCount = Math.round(message.errorCount);
    }
    if (message.errorHistory?.length) {
      obj.errorHistory = message.errorHistory.map((e) => ErrorRecord.toJSON(e));
    }
    if (message.currentTask !== undefined) {
      obj.currentTask = CurrentTask.toJSON(message.currentTask);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkerInfo>, I>>(base?: I): WorkerInfo {
    return WorkerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkerInfo>, I>>(object: I): WorkerInfo {
    const message = createBaseWorkerInfo();
    message.index = object.index ?? 0;
    message.browserOptions = (object.browserOptions !== undefined && object.browserOptions !== null)
      ? BrowserOptions.fromPartial(object.browserOptions)
      : undefined;
    message.health = object.health ?? 0;
    message.processedCount = object.processedCount ?? 0;
    message.errorCount = object.errorCount ?? 0;
    message.errorHistory = object.errorHistory?.map((e) => ErrorRecord.fromPartial(e)) || [];
    message.currentTask = (object.currentTask !== undefined && object.currentTask !== null)
      ? CurrentTask.fromPartial(object.currentTask)
      : undefined;
    return message;
  },
};

function createBasePendingTask(): PendingTask {
  return { taskId: "", url: "", labels: [], correlationId: undefined, enqueuedAt: "", queuedMs: 0, retryCount: 0 };
}

export const PendingTask: MessageFns<PendingTask> = {
  encode(message: PendingTask, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(26).string(v!);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(34).string(message.correlationId);
    }
    if (message.enqueuedAt !== "") {
      writer.uint32(42).string(message.enqueuedAt);
    }
    if (message.queuedMs !== 0) {
      writer.uint32(48).int32(message.queuedMs);
    }
    if (message.retryCount !== 0) {
      writer.uint32(56).int32(message.retryCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PendingTask {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePendingTask();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.enqueuedAt = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.queuedMs = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.retryCount = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PendingTask {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
      enqueuedAt: isSet(object.enqueuedAt)
        ? globalThis.String(object.enqueuedAt)
        : isSet(object.enqueued_at)
        ? globalThis.String(object.enqueued_at)
        : "",
      queuedMs: isSet(object.queuedMs)
        ? globalThis.Number(object.queuedMs)
        : isSet(object.queued_ms)
        ? globalThis.Number(object.queued_ms)
        : 0,
      retryCount: isSet(object.retryCount)
        ? globalThis.Number(object.retryCount)
        : isSet(object.retry_count)
        ? globalThis.Number(object.retry_count)
        : 0,
    };
  },

  toJSON(message: PendingTask): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    if (message.enqueuedAt !== "") {
      obj.enqueuedAt = message.enqueuedAt;
    }
    if (message.queuedMs !== 0) {
      obj.queuedMs = Math.round(message.queuedMs);
    }
    if (message.retryCount !== 0) {
      obj.retryCount = Math.round(message.retryCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PendingTask>, I>>(base?: I): PendingTask {
    return PendingTask.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PendingTask>, I>>(object: I): PendingTask {
    const message = createBasePendingTask();
    message.taskId = object.taskId ?? "";
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.correlationId = object.correlationId ?? undefined;
    message.enqueuedAt = object.enqueuedAt ?? "";
    message.queuedMs = object.queuedMs ?? 0;
    message.retryCount = object.retryCount ?? 0;
    return message;
  },
};

function createBaseProcessingTask(): ProcessingTask {
  return {
    taskId: "",
    url: "",
    labels: [],
    correlationId: undefined,
    enqueuedAt: "",
    queuedMs: 0,
    retryCount: 0,
    workerIndex: 0,
    startedAt: "",
    elapsedMs: 0,
  };
}

export const ProcessingTask: MessageFns<ProcessingTask> = {
  encode(message: ProcessingTask, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    for (const v of message.labels) {
      writer.uint32(26).string(v!);
    }
    if (message.correlationId !== undefined) {
      writer.uint32(34).string(message.correlationId);
    }
    if (message.enqueuedAt !== "") {
      writer.uint32(42).string(message.enqueuedAt);
    }
    if (message.queuedMs !== 0) {
      writer.uint32(48).int32(message.queuedMs);
    }
    if (message.retryCount !== 0) {
      writer.uint32(56).int32(message.retryCount);
    }
    if (message.workerIndex !== 0) {
      writer.uint32(64).int32(message.workerIndex);
    }
    if (message.startedAt !== "") {
      writer.uint32(74).string(message.startedAt);
    }
    if (message.elapsedMs !== 0) {
      writer.uint32(80).int32(message.elapsedMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessingTask {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseProcessingTask();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.taskId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.labels.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.correlationId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.enqueuedAt = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.queuedMs = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.retryCount = reader.int32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.workerIndex = reader.int32();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.startedAt = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.elapsedMs = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ProcessingTask {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : undefined,
      enqueuedAt: isSet(object.enqueuedAt)
        ? globalThis.String(object.enqueuedAt)
        : isSet(object.enqueued_at)
        ? globalThis.String(object.enqueued_at)
        : "",
      queuedMs: isSet(object.queuedMs)
        ? globalThis.Number(object.queuedMs)
        : isSet(object.queued_ms)
        ? globalThis.Number(object.queued_ms)
        : 0,
      retryCount: isSet(object.retryCount)
        ? globalThis.Number(object.retryCount)
        : isSet(object.retry_count)
        ? globalThis.Number(object.retry_count)
        : 0,
      workerIndex: isSet(object.workerIndex)
        ? globalThis.Number(object.workerIndex)
        : isSet(object.worker_index)
        ? globalThis.Number(object.worker_index)
        : 0,
      startedAt: isSet(object.startedAt)
        ? globalThis.String(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.String(object.started_at)
        : "",
      elapsedMs: isSet(object.elapsedMs)
        ? globalThis.Number(object.elapsedMs)
        : isSet(object.elapsed_ms)
        ? globalThis.Number(object.elapsed_ms)
        : 0,
    };
  },

  toJSON(message: ProcessingTask): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.correlationId !== undefined) {
      obj.correlationId = message.correlationId;
    }
    if (message.enqueuedAt !== "") {
      obj.enqueuedAt = message.enqueuedAt;
    }
    if (message.queuedMs !== 0) {
      obj.queuedMs = Math.round(message.queuedMs);
    }
    if (message.retryCount !== 0) {
      obj.retryCount = Math.round(message.retryCount);
    }
    if (message.workerIndex !== 0) {
      obj.workerIndex = Math.round(message.workerIndex);
    }
    if (message.startedAt !== "") {
      obj.startedAt = message.startedAt;
    }
    if (message.elapsedMs !== 0) {
      obj.elapsedMs = Math.round(message.elapsedMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessingTask>, I>>(base?: I): ProcessingTask {
    return ProcessingTask.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessingTask>, I>>(object: I): ProcessingTask {
    const message = createBaseProcessingTask();
    message.taskId = object.taskId ?? "";
    message.url = object.url ?? "";
    message.labels = object.labels?.map((e) => e) || [];
    message.correlationId = object.correlationId ?? undefined;
    message.enqueuedAt = object.enqueuedAt ?? "";
    message.queuedMs = object.queuedMs ?? 0;
    message.retryCount = object.retryCount ?? 0;
    message.workerIndex = object.workerIndex ?? 0;
    message.startedAt = object.startedAt ?? "";
    message.elapsedMs = object.elapsedMs ?? 0;
    return message;
  },
};

function createBaseQueueSnapshot(): QueueSnapshot {
  return { pendingTasks: [], processingTasks: [] };
}

export const QueueSnapshot: MessageFns<QueueSnapshot> = {
  encode(message: QueueSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.pendingTasks) {
      PendingTask.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.processingTasks) {
      ProcessingTask.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueueSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueueSnapshot();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.pendingTasks.push(PendingTask.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.processingTasks.push(ProcessingTask.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueueSnapshot {
    return {
      pendingTasks: globalThis.Array.isArray(object?.pendingTasks)
        ? object.pendingTasks.map((e: any) => PendingTask.fromJSON(e))
        : globalThis.Array.isArray(object?.pending_tasks)
        ? object.pending_tasks.map((e: any) => PendingTask.fromJSON(e))
        : [],
      processingTasks: globalThis.Array.isArray(object?.processingTasks)
        ? object.processingTasks.map((e: any) => ProcessingTask.fromJSON(e))
        : globalThis.Array.isArray(object?.processing_tasks)
        ? object.processing_tasks.map((e: any) => ProcessingTask.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueueSnapshot): unknown {
    const obj: any = {};
    if (message.pendingTasks?.length) {
      obj.pendingTasks = message.pendingTasks.map((e) => PendingTask.toJSON(e));
    }
    if (message.processingTasks?.length) {
      obj.processingTasks = message.processingTasks.map((e) => ProcessingTask.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueueSnapshot>, I>>(base?: I): QueueSnapshot {
    return QueueSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueueSnapshot>, I>>(object: I): QueueSnapshot {
    const message = createBaseQueueSnapshot();
    message.pendingTasks = object.pendingTasks?.map((e) => PendingTask.fromPartial(e)) || [];
    message.processingTasks = object.processingTasks?.map((e) => ProcessingTask.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBuildInfo(): BuildInfo {
  return { version: "", revision: "", buildTime: "" };
}

export const BuildInfo: MessageFns<BuildInfo> = {
  encode(message: BuildInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.revision !== "") {
      writer.uint32(18).string(message.revision);
    }
    if (message.buildTime !== "") {
      writer.uint32(26).string(message.buildTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBuildInfo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.version = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.revision = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.buildTime = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BuildInfo {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      revision: isSet(object.revision) ? globalThis.String(object.revision) : "",
      buildTime: isSet(object.buildTime)
        ? globalThis.String(object.buildTime)
        : isSet(object.build_time)
        ? globalThis.String(object.build_time)
        : "",
    };
  },

  toJSON(message: BuildInfo): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.revision !== "") {
      obj.revision = message.revision;
    }
    if (message.buildTime !== "") {
      obj.buildTime = message.buildTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildInfo>, I>>(base?: I): BuildInfo {
    return BuildInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildInfo>, I>>(object: I): BuildInfo {
    const message = createBaseBuildInfo();
    message.version = object.version ?? "";
    message.revision = object.revision ?? "";
    message.buildTime = object.buildTime ?? "";
    return message;
  },
};

function createBaseServerLimits(): ServerLimits {
  return { maxResponseBytes: 0, maxTaskBytes: 0, maxPendingRequests: 0 };
}

export const ServerLimits: MessageFns<ServerLimits> = {
  encode(message: ServerLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxResponseBytes !== 0) {
      writer.uint32(8).int64(message.maxResponseBytes);
    }
    if (message.maxTaskBytes !== 0) {
      writer.uint32(16).int64(message.maxTaskBytes);
    }
    if (message.maxPendingRequests !== 0) {
      writer.uint32(24).int32(message.maxPendingRequests);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseServerLimits();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.maxResponseBytes = longToNumber(reader.int64());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.maxTaskBytes = longToNumber(reader.int64());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.maxPendingRequests = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ServerLimits {
    return {
      maxResponseBytes: isSet(object.maxResponseBytes)
        ? globalThis.Number(object.maxResponseBytes)
        : isSet(object.max_response_bytes)
        ? globalThis.Number(object.max_response_bytes)
        : 0,
      maxTaskBytes: isSet(object.maxTaskBytes)
        ? globalThis.Number(object.maxTaskBytes)
        : isSet(object.max_task_bytes)
        ? globalThis.Number(object.max_task_bytes)
        : 0,
      maxPendingRequests: isSet(object.maxPendingRequests)
        ? globalThis.Number(object.maxPendingRequests)
        : isSet(object.max_pending_requests)
        ? globalThis.Number(object.max_pending_requests)
        : 0,
    };
  },

  toJSON(message: ServerLimits): unknown {
    const obj: any = {};
    if (message.maxResponseBytes !== 0) {
      obj.maxResponseBytes = Math.round(message.maxResponseBytes);
    }
    if (message.maxTaskBytes !== 0) {
      obj.maxTaskBytes = Math.round(message.maxTaskBytes);
    }
    if (message.maxPendingRequests !== 0) {
      obj.maxPendingRequests = Math.round(message.maxPendingRequests);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerLimits>, I>>(base?: I): ServerLimits {
    return ServerLimits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerLimits>, I>>(object: I): ServerLimits {
    const message = createBaseServerLimits();
    message.maxResponseBytes = object.maxResponseBytes ?? 0;
    message.maxTaskBytes = object.maxTaskBytes ?? 0;
    message.maxPendingRequests = object.maxPendingRequests ?? 0;
    return message;
  },
};

function createBaseGetStatusResponse(): GetStatusResponse {
  return {
    pending: 0,
    processing: 0,
    succeeded: 0,
    failed: 0,
    operationalWorkers: 0,
    totalWorkers: 0,
    isRunning: false,
    isDegraded: false,
    workers: [],
    queue: undefined,
    build: undefined,
    limits: undefined,
  };
}

export const GetStatusResponse: MessageFns<GetStatusResponse> = {
  encode(message: GetStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pending !== 0) {
      writer.uint32(8).int32(message.pending);
    }
    if (message.processing !== 0) {
      writer.uint32(16).int32(message.processing);
    }
    if (message.succeeded !== 0) {
      writer.uint32(24).int32(message.succeeded);
    }
    if (message.failed !== 0) {
      writer.uint32(32).int32(message.failed);
    }
    if (message.operationalWorkers !== 0) {
      writer.uint32(40).int32(message.operationalWorkers);
    }
    if (message.totalWorkers !== 0) {
      writer.uint32(48).int32(message.totalWorkers);
    }
    if (message.isRunning !== false) {
      writer.uint32(56).bool(message.isRunning);
    }
    if (message.isDegraded !== false) {
      writer.uint32(64).bool(message.isDegraded);
    }
    for (const v of message.workers) {
      WorkerInfo.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.queue !== undefined) {
      QueueSnapshot.encode(message.queue, writer.uint32(82).fork()).join();
    }
    if (message.build !== undefined) {
      BuildInfo.encode(message.build, writer.uint32(90).fork()).join();
    }
    if (message.limits !== undefined) {
      ServerLimits.encode(message.limits, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetStatusResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.pending = reader.int32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.processing = reader.int32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.succeeded = reader.int32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.failed = reader.int32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.operationalWorkers = reader.int32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.totalWorkers = reader.int32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.isRunning = reader.bool();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.isDegraded = reader.bool();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.workers.push(WorkerInfo.decode(reader, reader.uint32()));
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.queue = QueueSnapshot.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.build = BuildInfo.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.limits = ServerLimits.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetStatusResponse {
    return {
      pending: isSet(object.pending) ? globalThis.Number(object.pending) : 0,
      processing: isSet(object.processing) ? globalThis.Number(object.processing) : 0,
      succeeded: isSet(object.succeeded) ? globalThis.Number(object.succeeded) : 0,
      failed: isSet(object.failed) ? globalThis.Number(object.failed) : 0,
      operationalWorkers: isSet(object.operationalWorkers)
        ? globalThis.Number(object.operationalWorkers)
        : isSet(object.operational_workers)
        ? globalThis.Number(object.operational_workers)
        : 0,
      totalWorkers: isSet(object.totalWorkers)
        ? globalThis.Number(object.totalWorkers)
        : isSet(object.total_workers)
        ? globalThis.Number(object.total_workers)
        : 0,
      isRunning: isSet(object.isRunning)
        ? globalThis.Boolean(object.isRunning)
        : isSet(object.is_running)
        ? globalThis.Boolean(object.is_running)
        : false,
      isDegraded: isSet(object.isDegraded)
        ? globalThis.Boolean(object.isDegraded)
        : isSet(object.is_degraded)
        ? globalThis.Boolean(object.is_degraded)
        : false,
      workers: globalThis.Array.isArray(object?.workers)
        ? object.workers.map((e: any) => WorkerInfo.fromJSON(e))
        : [],
      queue: isSet(object.queue) ? QueueSnapshot.fromJSON(object.queue) : undefined,
      build: isSet(object.build) ? BuildInfo.fromJSON(object.build) : undefined,
      limits: isSet(object.limits) ? ServerLimits.fromJSON(object.limits) : undefined,
    };
  },

  toJSON(message: GetStatusResponse): unknown {
    const obj: any = {};
    if (message.pending !== 0) {
      obj.pending = Math.round(message.pending);
    }
    if (message.processing !== 0) {
      obj.processing = Math.round(message.processing);
    }
    if (message.succeeded !== 0) {
      obj.succeeded = Math.round(message.succeeded);
    }
    if (message.failed !== 0) {
      obj.failed = Math.round(message.failed);
    }
    if (message.operationalWorkers !== 0) {
      obj.operationalWorkers = Math.round(message.operationalWorkers);
    }
    if (message.totalWorkers !== 0) {
      obj.totalWorkers = Math.round(message.totalWorkers);
    }
    if (message.isRunning !== false) {
      obj.isRunning = message.isRunning;
    }
    if (message.isDegraded !== false) {
      obj.isDegraded = message.isDegraded;
    }
    if (message.workers?.length) {
      obj.workers = message.workers.map((e) => WorkerInfo.toJSON(e));
    }
    if (message.queue !== undefined) {
      obj.queue = QueueSnapshot.toJSON(message.queue);
    }
    if (message.build !== undefined) {
      obj.build = BuildInfo.toJSON(message.build);
    }
    if (message.limits !== undefined) {
      obj.limits = ServerLimits.toJSON(message.limits);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStatusResponse>, I>>(base?: I): GetStatusResponse {
    return GetStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStatusResponse>, I>>(object: I): GetStatusResponse {
    const message = createBaseGetStatusResponse();
    message.pending = object.pending ?? 0;
    message.processing = object.processing ?? 0;
    message.succeeded = object.succeeded ?? 0;
    message.failed = object.failed ?? 0;
    message.operationalWorkers = object.operationalWorkers ?? 0;
    message.totalWorkers = object.totalWorkers ?? 0;
    message.isRunning = object.isRunning ?? false;
    message.isDegraded = object.isDegraded ?? false;
    message.workers = object.workers?.map((e) => WorkerInfo.fromPartial(e)) || [];
    message.queue = (object.queue !== undefined && object.queue !== null)
      ? QueueSnapshot.fromPartial(object.queue)
      : undefined;
    message.build = (object.build !== undefined && object.build !== null)
      ? BuildInfo.fromPartial(object.build)
      : undefined;
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? ServerLimits.fromPartial(object.limits)
      : undefined;
    return message;
  },
};

/**
 * BrowserHive の capture API。
 *
 * HTTP との対応で決めたことが 2 つある:
 *
 *   1. 202 を state に移した。HTTP では「受理 = 202」「取得は処理中なら 202、
 *      完了で 200」とステータスコードで進行を表していた。gRPC に 202 は無いので、
 *      GetCaptureResponse.state に持たせる。呼ぶ側は code ではなく state を見る。
 *   2. 失敗は gRPC の status code で返す。RFC 7807 の Problem は落とした。
 *      対応は request-mapper / handlers 側のコメントに書いてある。
 */
export type CaptureServiceService = typeof CaptureServiceService;
export const CaptureServiceService = {
  /**
   * capture を投入する。fire-and-forget —— 受理した時点で返り、実際の capture は
   * worker pool が非同期に進める。結果は GetCapture で取る。
   */
  submitCapture: {
    path: "/browserhive.v1.CaptureService/SubmitCapture" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SubmitCaptureRequest): Buffer => Buffer.from(SubmitCaptureRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SubmitCaptureRequest => SubmitCaptureRequest.decode(value),
    responseSerialize: (value: SubmitCaptureResponse): Buffer =>
      Buffer.from(SubmitCaptureResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SubmitCaptureResponse => SubmitCaptureResponse.decode(value),
  },
  /**
   * 投入した capture の現在。まだ終わっていなければ state が PENDING か PROCESSING で
   * report は空。
   */
  getCapture: {
    path: "/browserhive.v1.CaptureService/GetCapture" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetCaptureRequest): Buffer => Buffer.from(GetCaptureRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetCaptureRequest => GetCaptureRequest.decode(value),
    responseSerialize: (value: GetCaptureResponse): Buffer => Buffer.from(GetCaptureResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetCaptureResponse => GetCaptureResponse.decode(value),
  },
  /** キューと coordinator の現在。 */
  getStatus: {
    path: "/browserhive.v1.CaptureService/GetStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetStatusRequest): Buffer => Buffer.from(GetStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetStatusRequest => GetStatusRequest.decode(value),
    responseSerialize: (value: GetStatusResponse): Buffer => Buffer.from(GetStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetStatusResponse => GetStatusResponse.decode(value),
  },
} as const;

export interface CaptureServiceServer extends UntypedServiceImplementation {
  /**
   * capture を投入する。fire-and-forget —— 受理した時点で返り、実際の capture は
   * worker pool が非同期に進める。結果は GetCapture で取る。
   */
  submitCapture: handleUnaryCall<SubmitCaptureRequest, SubmitCaptureResponse>;
  /**
   * 投入した capture の現在。まだ終わっていなければ state が PENDING か PROCESSING で
   * report は空。
   */
  getCapture: handleUnaryCall<GetCaptureRequest, GetCaptureResponse>;
  /** キューと coordinator の現在。 */
  getStatus: handleUnaryCall<GetStatusRequest, GetStatusResponse>;
}

export interface CaptureServiceClient extends Client {
  /**
   * capture を投入する。fire-and-forget —— 受理した時点で返り、実際の capture は
   * worker pool が非同期に進める。結果は GetCapture で取る。
   */
  submitCapture(
    request: SubmitCaptureRequest,
    callback: (error: ServiceError | null, response: SubmitCaptureResponse) => void,
  ): ClientUnaryCall;
  submitCapture(
    request: SubmitCaptureRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SubmitCaptureResponse) => void,
  ): ClientUnaryCall;
  submitCapture(
    request: SubmitCaptureRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SubmitCaptureResponse) => void,
  ): ClientUnaryCall;
  /**
   * 投入した capture の現在。まだ終わっていなければ state が PENDING か PROCESSING で
   * report は空。
   */
  getCapture(
    request: GetCaptureRequest,
    callback: (error: ServiceError | null, response: GetCaptureResponse) => void,
  ): ClientUnaryCall;
  getCapture(
    request: GetCaptureRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetCaptureResponse) => void,
  ): ClientUnaryCall;
  getCapture(
    request: GetCaptureRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetCaptureResponse) => void,
  ): ClientUnaryCall;
  /** キューと coordinator の現在。 */
  getStatus(
    request: GetStatusRequest,
    callback: (error: ServiceError | null, response: GetStatusResponse) => void,
  ): ClientUnaryCall;
  getStatus(
    request: GetStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetStatusResponse) => void,
  ): ClientUnaryCall;
  getStatus(
    request: GetStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetStatusResponse) => void,
  ): ClientUnaryCall;
}

export const CaptureServiceClient = makeGenericClientConstructor(
  CaptureServiceService,
  "browserhive.v1.CaptureService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): CaptureServiceClient;
  service: typeof CaptureServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
